# Portfolio Automation System

## Project overview
Azure-native automated portfolio analysis and paper trade execution pipeline. Single-user personal system. NOT for live trading. All trade decisions require human approval via Teams adaptive card.

> **Open work / where we left off:** see [`FOLLOWUPS.md`](FOLLOWUPS.md) at the repo root.

## Architecture decisions (do not deviate without discussion)
- **Azure AI Foundry** for Claude API — project: Portfolio-Analysis, resource: resource-portfolio-analysis (East US 2, Claude not available in East US). API key auth (FoundryApiKey in KV). Endpoints in Function App settings: FOUNDRY_ENDPOINT, FOUNDRY_OPENAI_ENDPOINT. Model: claude-sonnet-4-6, temp 0.2
- **Alpaca paper account** is the canonical source of truth for portfolio positions and balances (`portfolio.positions[]`, `portfolio.balances`, `paper_account` block). Also used for Phase 2 paper-trade execution. REST API, no VM needed. (E*TRADE was removed in commit `bc60604` — its OAuth-1.0a tokens expired daily and the integration was dropped; `src/shared/clients/etrade.py` is dead code retained only as historical reference.)
- **FMP (Financial Modeling Prep)** for fundamentals, earnings, ETF look-through, EOD prices, stock news, and fallback congressional trades (senate/house) — free tier, 250 req/day
- **FRED** for macro indicators (35 series collected, including 6 labor-market series feeding `labor_signals` scorecard; `CHPMINDXM` deprecated — to revisit) — free, no practical limit
- **Quiver Quantitative** (primary alternative-data source) for congressional trades, lobbying, government contracts, wikipedia attention — hobbyist tier, `Authorization: Token <key>` header
- **Finnhub** for financial market news and company news — free tier, 60 calls/min
- **Azure AI Search** (Free tier) for semantic memory recall — Phase 1.5, after 60-90 days of data

## Tech stack
- Language: Python 3.11
- IaC: Bicep (no manual portal config for prod)
- CI/CD: GitHub Actions with OIDC federated credentials (no secrets in GitHub)
- Linting: ruff
- Testing: pytest
- Azure Functions: Consumption plan, Linux, timer + blob triggers
- Static Web App: Free SKU, Entra ID Easy Auth (preconfigured provider + invitation-based `owner` role — no custom app registration; managed functions can't use MI/KV app-setting references on any plan, see "Key Vault secrets" above), managed Python /api functions
- Storage: single account (stpfautoprod) with Blob + Table Storage
- Key Vault: kv-pfauto-prod, RBAC auth, Managed Identity only — no service principals or connection strings in code
- Monitoring: Application Insights (workspace-based)

## Resource naming
- Resource group: rg-portfolio-automation-prod
- Storage account: stpfautoprod
- Key Vault: kv-pfauto-prod
- Function App: func-pfauto (hosts collector + analyzer; future executor endpoint)
- Static Web App: swa-pfauto (single pane of glass — report viewer + per-trade approval, Entra ID auth)
- App Insights: appi-pfauto-prod
- Log Analytics: log-pfauto-prod

## Repository structure
```
portfolio-automation/
├── infra/                    # Bicep templates
│   ├── main.bicep
│   ├── modules/              # storage, keyvault, functionapp, staticwebapp, monitoring
│   ├── parameters.prod.json
│   └── deploy.sh
├── src/                      # func-pfauto: all Azure Functions + shared libs
│   ├── function_app.py       # single entry point — registers every trigger (v2 model)
│   ├── host.json             # functionTimeout 00:40:00 (Flex Consumption)
│   ├── collector/            # Timer-triggered (09:00 ET), collects from all APIs
│   ├── analyzer/             # Blob-triggered (EventGrid), assembles context, calls Claude
│   ├── executor/             # HTTP + timer, Phase 2 / auto paper execution + retries
│   ├── flex/                 # Intraday catalyst Flex engine (timer, FLEX_ENABLED-gated)
│   ├── seeder/               # One-time idempotent Alpaca paper seeding (HTTP)
│   ├── shared/               # Common utilities + API clients + pure decision modules
│   │   ├── clients/          # alpaca, fmp, fred, quiver, finnhub, foundry, (etrade dead)
│   │   ├── quadrants.py      # ROLE-BASED membership: resolves AMPLIFIER/DAMPER/CONCENTRATE/CORE_ROSTER from sleeve-roles.json `selected` (roster_revision_2026-07)
│   │   ├── reference_execution.py  # Finding 2 — reconcile (band enforcement, D1-D3)
│   │   ├── trade_validation.py     # #28 — Tier-1 trade validator (V1-V4)
│   │   ├── overrides.py            # OVERRIDE_SCHEMA_V1_1 validate_overrides (Tier-2)
│   │   ├── timeutil.py             # today_et / now_et (zoneinfo; #29)
│   │   ├── storage.py, keyvault.py # blob/table I/O + KV via Managed Identity
│   └── config/               # NOTE: config lives UNDER src/, not at repo root
│       ├── project-instructions.md    # Claude system prompt for analysis
│       ├── macro-series.json          # FRED series IDs
│       ├── risk-limits.json           # canonical risk limits — source of truth for reference_weights + override/exec/validator configs
│       ├── sleeve-roles.json          # ROLE/POOL config: selected incumbent per role + selection/intl tunables (roster_revision_2026-07)
│       ├── divergence-config.json     # thresholds for the divergences[] tension detector
│       ├── fomc-stance.json           # manually-maintained FOMC policy stance (as_of gates the manual override layer)
│       ├── flex-candidates.json       # seed non-held flex watchlist
│       └── portfolio.json             # Alpaca-unreachable fallback (positions only)
├── web/                      # Static Web App: single pane of glass
│   ├── today.html/js, performance.html/js, history.html/js, portfolio.html/js, index.html
│   ├── app.js, styles.css
│   ├── staticwebapp.config.json  # Entra ID auth + route protection
│   └── api/                  # SWA managed Python Functions (HTTP only)
├── docs/
│   └── specs/                # Architecture spec + companion docs (.md + .docx) — NO runbooks/ dir; ops notes live in CLAUDE.md "Deployment lessons"
├── scripts/                  # dev helpers: docx_to_md.py, run_fmp_smoke.py
├── tests/                    # pytest suite (PYTHONPATH=src)
├── .github/workflows/
│   ├── ci.yml               # pytest + ruff on PR/push
│   ├── deploy-infra.yml
│   ├── deploy-code.yml      # func-pfauto (collector/analyzer/executor/flex/seeder)
│   └── deploy-web.yml       # swa-pfauto (frontend + managed API)
├── FOLLOWUPS.md              # open work / where we left off
├── CLAUDE.md                 # This file
└── README.md
```

## Key Vault secrets (10 active)
FmpApiKey, FredApiKey, AlpacaApiKey, AlpacaApiSecret, FoundryApiKey, FinnhubApiKey, QuiverApiKey — read at RUNTIME by func-pfauto via Managed Identity (`KEY_VAULT_URI` + `shared/keyvault.py`).

`swa-storage-connection-string`, `swa-func-master-key`, `github-learning-pat` — the `web/api` managed-function app settings (SWA hardening batch, FOLLOWUPS #2, 2026-07-11; the PAT added by the Learning Loop batch, FOLLOWUPS #13/#32, 2026-07-12). These are resolved at **DEPLOY TIME**, not runtime: `az bicep`'s `getSecret()` (`infra/main.bicep`, against `kv-pfauto-prod` with `enabledForTemplateDeployment: true`) bakes the current value into a plain `staticwebapp.bicep` app setting on every infra deploy. This is a different mechanism from the func-pfauto secrets above because it has to be — Azure Static Web Apps *managed functions* support neither Key Vault app-setting references nor managed identity, on any plan (verified against Microsoft Learn 2026-07-11). `github-learning-pat` is a fine-grained GitHub PAT scoped to this repo only (`contents:write` + `pull_requests:write`, no merge/admin — see "Learning Loop" below). Seed/rotate via `scripts/seed-swa-secrets.sh`.

(Note: `MassiveApiKey` and the four `Etrade*` secrets may still exist in KV as soft-deletable leftovers from deprecated integrations — safe to delete manually. The collector no longer fetches them.)

If Alpaca is unreachable the collector falls back to `src/config/portfolio.json` (positions only — dollar gains will be zero in that mode).

## Data flow — Phase 1
1. Timer fires collector at 09:00 ET weekdays (NCRONTAB: `0 0 9 * * 1-5`, ET-local via `TZ=America/New_York` app setting — no DST drift). **NOTE**: this is Linux Consumption, so the IANA `TZ` setting is required. The Windows-only `WEBSITE_TIME_ZONE` is silently ignored on Linux and caused crons to fire 4.5h early (at 09:00 UTC = 05:00 ET) until commit 6f42f1a.
2. Collector reads secrets from Key Vault via Managed Identity
3. Collector calls Alpaca paper account (positions + balances drive the canonical `portfolio.positions[]` / `portfolio.balances`; `paper_account` block also retained with cash, buying_power, equity, last_equity, position list — falls back to `src/config/portfolio.json` if Alpaca unreachable; NOTE `paper_account.positions[]` uses the Alpaca-native `qty` field, while `portfolio.positions[]` uses `quantity` — code reading positions must accept both, see the 2026-07-07 held_qty incident, FOLLOWUPS #33), FMP (fundamentals, earnings, EOD prices — the price-fetch universe is held tickers ∪ every role's `selected` incumbent (`shared/quadrants.selected_core_members()`, 2026-07-13 audit finding 1 — an unheld reference target previously had no price, no gap row, and no way for band enforcement to synthesize its buy) ∪ the ETF watchlist ∪ flex candidates, assembled by `collector._build_price_universe()` —, ETF data, stock news, fallback congressional), FRED (35 macro series, deep-history for bond + labor scorecards), Quiver (congressional trades, lobbying, gov contracts — **lobbying and gov_contracts are filtered client-side to portfolio tickers ∪ ETF watchlist and last 90 days; raw responses are ~20K rows/~16 MB and would blow past Claude's 1 M-token limit**), Finnhub (market news, company news)
4. Collector writes full JSON snapshot to `daily-snapshots/YYYY-MM-DD.json` blob
5. Collector writes denormalized rows to 6 Table Storage tables (PortfolioHistory, FundamentalsHistory, MacroHistory, ETFLookthroughHistory, SentimentHistory, TradeHistory)
6. Blob trigger fires analyzer
7. Analyzer reads today's snapshot + queries tables for historical trends + loads last 5 reports for continuity
8. Analyzer calls Claude via Foundry (Sonnet 4.6, temp 0.2, max_tokens 8000). Responses are **streamed** (`shared/clients/foundry.py`) to survive Azure's ~4-min outbound idle drop on long generations (PR #7, 2026-07-02).
9. Analyzer parses response: markdown report + structured trade recommendations JSON. Then, deterministically and post-model: validates `overrides[]` (Tier-2, `shared/overrides.py` — session 2026-07-15 Task E1: each record's `direction` is now DERIVED from the sleeve's block + gap sign via `shared/reference_execution.derive_override_direction`, not trusted from the model's claim; a disagreement is corrected-and-flagged, never rejected outright, and both the effective `direction` and the model's original `declared_direction` persist to OverrideHistory). Then **two-pass Tier-1 validation around band enforcement** (session 2026-07-15 Task B1 — replaces a single pass that let a soon-to-be-rejected buy's notional starve enforcement cash, see "Deployment lessons" below): **pass 1** runs the **Tier-1 trade validator** (`shared/trade_validation.validate_trades` — V1 gate/roster, **V1.5 selected-member-only buys** (a BUY of a role's pool member that isn't that role's `selected` incumbent is rejected — the role-based roster otherwise lets a non-selected pool member, e.g. SOXX when `semis.selected = SMH`, pass straight through; the lone exception is `intl_leader`, whose `selected` follows `intl_governance.leader_pick` automatically), V2 exemption, V3 window (now also rejecting a BUY with no matching reference/gap row outright, rather than silently skipping the window), V4 mechanical) over the model's raw trades and drops what it would reject; **band enforcement** (`shared/reference_execution.reconcile` — synthesizes de-risk shortfall trades toward reference, now also treating a `LEGACY_EXITS` sell as de-risk, Task D1) runs against the pass-1 SURVIVORS (so a rejected buy's dollars are never mistaken for spent cash) and now counts an off-roster held name's sell proceeds toward available cash too (Task B2) while still never targeting an off-roster name for synthesis; **pass 2** re-validates the full merged list (survivors + synthesized trades) so cumulative checks see the final list. Rejections from both passes combine into the report addendum, deduped by trade id. Either pass crashing sets `validation_error` and the auto-executor refuses the file (fail-closed). **Pass-1 clamps now survive pass 2** (session 2026-08-01, Task A — closes a seam this same two-pass design left open): before this fix, a trade `shared/trade_validation.validate_trades` clamped in pass 1 (a sell trimmed toward its sleeve floor) would re-enter pass 2 already inside its window, re-validate cleanly, and have its `"clamped"` stamp silently overwritten to `"passed"` — dropping it from `trade_validation.summary.clamped` and suppressing the `_validation_addendum` report section entirely (the 07-30 KMLM incident: proposed sell 182, submitted 179, no record anywhere explained the gap). `validate_trades` now preserves a trade's incoming `"clamped"` status and reasons (merged with any new ones) across a second pass instead of overwriting it; `analyzer.handler`'s `combined_summary["clamped"]` needed no code change — it now correctly counts both passes' clamps as a direct consequence.
10. Writes report to `daily-reports/YYYY-MM-DD.md`, trades to `daily-trades/YYYY-MM-DD.json` (with `trade_validation`/`reference_execution` blocks and an optional `watch_candidates` key), OverrideHistory rows. **`watch_candidates` contract:** optional top-level key in `daily-trades/{date}.json` — `[{"symbol": "XYZ", "reason": "one line"}, ...]`, max 6 entries, sanitized by `_split_response` (malformed entries dropped with a log line, never fatal; the trades file itself is never invalidated by a bad entry). The executor and trade validator ignore this key entirely — it is consumed only by the NEXT day's collector run to seed dynamic flex candidates. Each `flex_candidates` FMP profile in the snapshot's `flex_candidates[]` block carries `source: "static"|"dynamic"` indicating whether the name came from `flex-candidates.json` or from the analyzer's prior `watch_candidates` emission.
11. Outputs surfaced in `swa-pfauto` (no Logic App delivery; user pulls report via web UI). Optional email/OneDrive copies can be added later if needed.

## Data flow — Phase 2 (paper execution)
There are TWO execution paths into the same `executor.execute_approvals`:

**Auto path (currently live — `AUTO_EXECUTE_ENABLED=true`, paper only):**
1. `auto_executor` timer fires 09:35 ET; retry nets at 10:05 + 11:05 ET (`auto_executor_retry`, #29) cover variable analyzer latency and missed opens
2. Reads `daily-trades/{ET-today}.json` (ET trading date via `shared/timeutil.today_et` — never UTC), treats every recommendation as approved, but **refuses** any file flagged by the Tier-1 validator (`validation_error`, rejected-stamped trades). Gated by the Alpaca market clock (defers if closed). Idempotent by a cache asymmetry (terminal outcomes cached; failures left uncached so retries re-attempt) + date-scoped `client_order_id`.
3. Executor connects to Alpaca paper API, places orders (sells before buys), applies the defensive sell filter (drops sells of unheld symbols; trims oversize qty). Writes `daily-executions/{date}.json` + TradeHistory.

**Manual path (Static Web App — for when auto is off or for live trading later):**
1. User signs into `swa-pfauto` with Entra ID work account (Easy Auth; allowed-role `owner`)
2. `/today` renders the report + trade table with per-row checkboxes
3. User selects 1…N trades and clicks **Approve selected**
4. SWA managed API (`web/api/`) records the decision in `approvals/YYYY-MM-DD.json` and calls the `func-pfauto` executor endpoint (function master key fetched via SWA MI → Key Vault reference)
5-6. Same executor → same `daily-executions/{date}.json` + TradeHistory; SWA `/today` polls and shows status inline

## Learning Loop (monthly strategy review + human-approval amendment channel)
**Spec: `docs/specs/Learning_Loop_v1.0.md`. Resolves FOLLOWUPS #13 + #32.** Where the
daily analyzer executes toward a fixed reference and grades its own trades, the
Learning Loop is the missing half: a monthly, high-reasoning review that proposes
*changes to the rules themselves* from the graded record, gated entirely behind human
approval. **Non-negotiable invariant: proposer ≠ approver.** Nothing in this system can
apply a change directly — every approved proposal becomes a GitHub pull request that a
human merges after CI passes; the automation credential cannot merge (branch protection,
not convention).

- **`src/learning/bundle.py`** assembles the reviewer's input: the trailing 35 days of
  daily reports (oldest dropped first if `LEARNING_BUNDLE_MAX_TOKENS` is exceeded — graded
  records and live config NEVER drop), capture-fine TradeHistory, every OverrideHistory
  layer, and live config fetched from **GitHub raw at master HEAD** (not the deployed
  function package — a proposal diff must apply to master) alongside the recorded
  `diff_base_sha`. `fetch_override_history` (session 2026-07-17, Task G-b) annotates each
  `layer: "override"` accepted/downsized row with `_direction_suspect` — true when
  `declared_direction` is absent, which is possible ONLY for a record from before the
  2026-07-15 Task E1 derived-direction fix (every accepted/downsized decision since then
  always populates both `direction` and `declared_direction`). A pre-E1 row's `direction`
  may be backwards (07-15 filed three de-risk holds — GLD/XLP/TLT — as `re_risk`), which
  would invert the sign Phase-5's counterfactual grading uses; the monthly reviewer must
  not treat a flagged row's direction/outcome as reliable signal without accounting for it.
- **`src/learning/handler.py`** (`learning_reviewer` timer, Saturdays 12:00 ET with an
  in-code first-Saturday-of-month gate; `learning_run` HTTP trigger for the manual
  button, 1/day rate-limited) calls the reviewer model — **`LEARNING_MODEL`, default
  `claude-sonnet-4-6`, the analyzer's EXISTING Foundry deployment** (no new deployment,
  no new quota; upgrading to Claude Fable 5 is a config flip once quota lands, FOLLOWUPS
  #40) — then validates the output before anything is stored as pending.
- **`src/learning/schema.py`** + **`src/learning/diffcheck.py`** (pure-Python unified-diff
  apply checker, no third-party dep) deterministically enforce the proposal contract:
  4 classes (0 Observation / 1 Prompt-text / 2 Parameter, needs `evidence_n >= 10` graded
  rows / 3 Structural, no diff — a spec draft + implementation brief instead), a
  **target-file allowlist** (`project-instructions.md`, `risk-limits.json`,
  `sleeve-roles.json`, `flex-candidates.json` — code/validator/infra are never a diff
  target), hard caps (≤3/cycle, ≤1 structural, reverts exempt), and the forced re-review
  rule (every amendment past its `review_by` date must get an explicit keep/revert/amend
  this cycle). **Any violation fails the whole cycle loudly** (`failed_validation`, raw
  output preserved) — nothing partially surfaces.
- **`web/api/learning_github.py`** is the ONLY thing that writes to GitHub, and only via
  PR: Approve re-validates a class 1-2 diff against CURRENT master (stale → the proposal
  regenerates next cycle, never force-applied), then branches, commits, and opens a PR
  with the proposal JSON verbatim in the body. Merge detection is lazy (checked on every
  `GET /api/learning/proposals`, cached ≥10 min) — no webhook in v1. On merge, the
  proposal flips to `applied` and an OverrideHistory row (layer `amendment`) is written
  so the loop eventually grades itself (mechanical grader deferred to FOLLOWUPS #38,
  ≥5 amendments).
- **Learning tab** (`web/learning.html`/`.js`) — nav entry rendered ONLY when
  `GET /api/learning/proposals` reports `LEARNING_PHASE >= 2` (`app.js::renderLearningNav`).
  Decision buttons render only at phase 3. **3-phase rollout** (`LEARNING_PHASE` app
  setting, ships `1`): 1 = dry-run (reviewer runs, no tab) → 2 = tab read-only → 3 = full
  loop. `POST /api/learning/decision` and `/run` require the authenticated principal to
  hold the `owner` role (the same role the platform's route rules already enforce), with
  an OPTIONAL tighter pin to one specific SWA user id via `OWNER_USER_ID` — that's SWA's
  own opaque `userId` from `/.auth/me` *after* signing in, **not an Entra object id**
  (the two are unrelated identifiers; a wrong value here silently denies everyone,
  including the real owner — fixed 2026-07-12 after exactly that shipped as
  `OWNER_OBJECT_ID` in PR #23's first draft). Empty/unset `OWNER_USER_ID` = roles-only
  mode.
- **Tables**: `LearningCycles` (one row per cycle, status/model/mode/narrative) and
  `LearningProposals` (one row per proposal, `status`: pending → approved/rejected/stale
  → applied) — see Table Storage schemas below.

## Table Storage schemas (10 tables)
- **PortfolioHistory**: PK=ticker, RK=date — positions, weights, P/L
- **FundamentalsHistory**: PK=ticker, RK=date — P/E, DCF, rating, earnings date
- **MacroHistory**: PK=series_id, RK=date — FRED values with deltas
- **ETFLookthroughHistory**: PK=etf_ticker, RK=date — holdings, country/sector allocation
- **SentimentHistory**: PK=date, RK=indicator — VIX, spreads, P/C ratios, percentiles
- **TradeHistory**: PK=year-month, RK=trade_id — full lifecycle from recommendation to 30/60/90d outcome. Phase C adds (write-once at recommendation, flex trades) the §7 reasoning enums `primary_trigger`/`thesis_type`/`trigger_evidence`/`catalyst_date`, and (stamped later by the collector at maturity) `price_at_rec`/`spy_at_rec`/`ret_Nd_pct`/`spy_ret_Nd_pct`/`excess_Nd_pp`/`call_correct_Nd`/`outcome_status`. Keys are **lowercase** across analyzer + executor + collector writes — Azure Tables is case-sensitive and upserts MERGE onto one entity (Phase C §9 casing fix).
- **OverrideHistory**: PK=year-month, RK=`OV-YYYYMMDD-NNN` — one row per override record the analyzer emitted (Phase 4, write-once): `outcome` (accepted/downsized/rejected), `validator_reasons`, `premise_challenged`, `sleeve` (V1_1), `direction`, `magnitude_pp`, `downsized`, `evidence`, `evidence_count`, `falsifier`, `falsifier_date`, `clean_data_only`, `enforced` (Finding-2 band enforcement fired on this sleeve). **Phase 5 (merged 2026-07-05) now STAMPS** `outcome_status`/`resolved_correct` at each record's `falsifier_date`, graded against the reference-path counterfactual ("did disagreeing beat obeying"); the collector maturity pass writes them. Also holds the roster-revision **`sleeve_switch`** / **`intl_leader_rotation`** rows (Task G, RK `SW-…`/`ILR-…`) graded vs the incumbent counterfactual at 30/60/90d, and the **`regime_suspect`** rows (FOLLOWUPS #12 Task C, RK `RS-YYYYMMDD-{bucket}`, one per suspect bucket per report day: `sleeve` = the bucket e.g. `Q3`, `favored_streak`, `streak_excess_pp`, `action` = increased/held/reduced that session — **not yet graded by any stamper**, see `quadrant_performance` above). The Learning Loop (FOLLOWUPS #13/#32) adds the **`amendment`** layer (RK `AMD-{proposal_id}`, written by `web/api` when a proposal's PR merges): `proposal_id`, `sleeve` = the amendment's `target_file`, `falsifier`, `review_by` — grading hooks also null until the mechanical amendment grader (FOLLOWUPS #38) is built.
- **SleeveSelectionState**: PK=`state`, RK=role_id (or `intl_governance`) — hysteresis streak (`challenger`/`streak`/`selected`) for the `sleeve_selection` scorecard + the intl `leader`/`composite` de-rotation state.
- **LearningCycles**: PK=year-month, RK=cycle date (`YYYY-MM-DD`) — one row per Learning Loop reviewer run: `trigger` (timer/manual), `status` (completed/failed_validation), `model`, `mode` (full/observation_only), `proposal_count`, `narrative`.
- **LearningProposals**: PK=year-month (of its cycle), RK=proposal id (`AMD-YYYY-MM-DD[-N]`) — one row per proposal: `class`, `title`, `change_summary`, `data_summary`, `target_file`, `diff`, `evidence`, `evidence_n`, `expected_effect`, `falsifier`, `review_by`, `spec_draft`/`implementation_brief` (class 3), `re_review_of`/`is_revert`, `diff_base_sha`, and the decision lifecycle: `status` (pending → approved/rejected/stale → applied), `decision`/`decision_reason`/`decided_at`/`decided_by`, `pr_url`/`pr_number`/`pr_last_checked`, `applied_at`.

## Snapshot analytics blocks (collector pre-computes; analyzer consumes)
Beyond raw API data, the collector injects pre-computed analysis blocks into each `daily-snapshots/{date}.json` so the analyzer reads conclusions, not raw series: `regional_rotation`, `bond_signals`, `labor_signals`, `market_shock`, `growth_axis`/`inflation_axis`/`regime_gate` (the deterministic quadrant axes + deployment gate, echoed not re-derived), `sleeve_selection` (role-member scorecard, Task E), `role_selection` (session 2026-07-15/17, Task C — see below), `intl_governance` (rotation/DXY-governed intl sleeve, Task F), and (Phase C) `performance` (account equity vs fully-invested SPY since inception + rolling 30/60/90d + `cash_pct`), `quadrant_performance` (FOLLOWUPS #12 — regime-call accountability, describe-only), `quadrant_allocation` (session 2026-07-17, Task D — see below), `functional_coverage` (session 2026-07-21, B3 — Table B, see below), `flex_quadrant` (session 2026-07-21, A1 — the flex engine's borderline-resolved quadrant, see below), `freshness` (session 2026-07-21, B4 — deterministic Data-Freshness table, see below) and `track_record` (hit-rate by layer/trigger/thesis at the 60d headline + confidence calibration, aggregated from stamped TradeHistory rows). Phase C 7a also maintains a compact `performance/equity-series.json` blob (collector-owned cache: backfilled once from snapshots, then append-only). The `performance` block also carries `excess_attribution` (session 2026-07-21, B5 — cash-vs-invested decomposition of the vs-SPY excess). New blocks added 2026-07-23 (FOLLOWUPS #17/#18 + Task C): `leading_growth` (9-signal leading-growth composite, describe-only — feeds `leading_vs_lagging_growth` divergence + growth-side `transition_watch`), `market_implied_quadrant` (cross-asset tape-implied quadrant + per-vote table, describe-only — works at borderline regimes, feeds `market_vs_macro_quadrant` divergence), `dollar_proxy` (FX-pairs-derived daily USD direction when DTWEXBGS >5d stale), `pnl_decomposition` (FIFO realized + current unrealized P&L split by `core_current`/`legacy_exits`/`off_roster_flex` — answers the inception-shortfall attribution question). New block added 2026-08-14 (Task D, decisions D-4/D-5): `thematic_conviction` (the LLM-probability-driven thematic overlay — ladder/caps/eligible-universe scaffold + confirm/release-hysteresis `active[]`/`pending[]` entries + Brier calibration, baked into `reference_weights` as a floor lift; see below).

**`execution_review`** (session 2026-07-15, Task A1 — a response to the 2026-07-14/15 MU incident, where a validated, submitted sell 403'd against a stale flex-engine GTC stop order and was silently re-proposed the next day with no visible failure anywhere) reads back the PRIOR trading day's `daily-executions/{date}.json` (looking back up to 7 days for the most recent one, so a weekend/holiday gap doesn't stall it) and reconciles each submitted order's ACTUAL terminal Alpaca state via `AlpacaClient.get_order`. Alpaca-only (FMP budget untouched), non-fatal (`{"available": False, "reason": ...}` on any failure — never loses the snapshot). Emits `{date, submitted, filled, failed[], unfilled[], available}` — `failed` covers both an order that never reached Alpaca (submission itself errored, e.g. the MU 403) and one that Alpaca terminally rejected/canceled/expired; `unfilled` covers an order still resting (not yet terminal) at the next day's collector run. The analyzer's prompt (Task A1 companion edit) surfaces any `failed`/`unfilled` entries under the Data Integrity Warning heading and must never assume a prior day's proposal executed — re-proposing a symbol that appears in `execution_review.failed`/`.unfilled` must say so explicitly.

**`execution_config`** (session 2026-07-17, Task B — #33(i) graduation) echoes `shared/reference_execution.py::effective_execution_config` — the SAME resolved numbers `reconcile`/`validate_trades` actually use (`gap_band_pp`, `max_magnitude_pp`, `de_risk_min_evidence`/`re_risk_min_evidence`, `tranche_pp_max`, `enforce`, `enforcement_turnover_max_pct`, `min_notional_usd`, `sleeve_floor_pct_of_core`) — into the snapshot, so the prompt never has to guess a config value from prose. Four consecutive report sessions had guessed wrong (assumed `tranche_pp_max` 3–5pp against a true 10.0; assumed `gap_band_pp` 1.0pp against a true 5.0 on 2026-07-17, which alone filed three unnecessary in-band overrides on GLD/XLP/TLT — all three sat inside the real 5pp band). The prompt now quotes every tranche/band/floor/min-notional/evidence-bar figure from `execution_config` verbatim and checks the band FIRST (an in-band gap is sheltered by construction — no override, no tranche obligation).

**`quadrant_allocation`** (session 2026-07-17, Task D) is the deterministic CURRENT-side counterpart to `reference_weights.by_quadrant` — Table A's "Current % of equity" column, precomputed to kill freehand quadrant-sum arithmetic (2026-07-16/17 reports leaked contradictory Table A's and a literal "wait — let me recompute carefully" into the markdown). `collector._build_quadrant_allocation` buckets every paper-account position via the shared `shared/quadrants.py::quadrant_allocation_bucket` — the SAME static tagging convention `_aggregate_by_quadrant` (the Reference column) already uses, so Current and Reference can never disagree about which bucket a dual-quadrant name's dollars belong to. Buckets: `Q1`-`Q4`, `intl`, `legacy_exits` (a held LEGACY_EXITS name — a dedicated row so a wind-down position is never folded into a quadrant it no longer represents), `off_roster` (a flex leftover like MU), `cash_sleeve` (SGOV + literal cash), `unmapped` (visibility safety net, never a silent drop). The Recommended (post-trade) column can't be precomputed at collect time — trades don't exist yet — so `analyzer._quadrant_allocation_addendum` computes it POST-model, applying the FINAL validated `trades[]` to the same buckets and appending a markdown table the prompt must quote verbatim.

**`series_deltas`** (session 2026-07-17, Task E — hardens Task F1's catalyst adjudication) reads back the prior trading day's snapshot (same non-fatal "look back up to 7 days" pattern as `execution_review`) and computes `{value, as_of, prior_value, prior_as_of, delta, new_print}` per tracked freshness-set macro series (GDPNow, core/headline CPI+PCE, DFF, DGS2, real 10Y, WTI/Brent, DXY, breakevens, HY OAS). Fixes a 2026-07-17 seam where the model's own recollection of a prior report's cited value was unreliable — it attributed a CPI flag to the wrong prior report and the wrong prior value, and hand-waved a third-party press figure that plainly disagreed with FRED. The prompt must now cite `series_deltas` — never memory — for every new-print/cadence/catalyst-resolution statement. **F1 weekend-walkback fix (session 2026-07-28, Task B):** the walkback loop called `read_snapshot` bare — it RAISES on a missing blob (unlike `read_executions`'s best-effort `None`), so a weekend/holiday gap (back=1 landing on a non-trading day) aborted the WHOLE 7-day walkback instead of continuing to the next date (observed 2026-07-27, a Monday). Fixed with a per-date `try/except: continue`, mirroring the identical pattern already used around `read_snapshot` in `_load_equity_spy_series`. `read_snapshot`'s raising contract is otherwise unchanged (other callers rely on it).

**Axis-direction confirmation (session 2026-07-28, Task A, decision D-2).** `growth_axis.direction` / `inflation_axis.direction` / `policy_axis.stance` are now the CONFIRMED classification, not the raw one — a label change (to ANY value, including `flat`) only reaches these fields (and therefore every downstream consumer: `active_quadrant`, `reference_weights`, `regime_gate`, `market_vs_macro_quadrant`, ...) after the RAW classification has persisted for **2 consecutive collector runs**. Motivating incident: 2026-07-28's growth axis flipped falling→rising purely because the oldest GDPNow vintage aged out of the six-vintage window while the newest print was actually LOWER, re-anchoring `reference_weights` from 55.9% Q3 to 72.5% Q1+Q2 overnight on a single print — member-selection already had an analogous hysteresis gate (`sleeve_selection`, 2.0pp/10 runs); regime sizing had none. Mechanics: `collector._confirm_axis_direction` (pure) + a new **`AxisDirectionState`** table (PK=`state`, RK=`growth`/`inflation`/`policy`, mirrors `SleeveSelectionState`'s load/save pattern exactly: `{raw_direction, confirmed_direction, raw_streak, confirmed_as_of}`). Each axis block gains `raw_direction` (this run's uncushioned read), `direction_pending` (bool), `raw_streak`, `confirmed_as_of` — **zero consumer code changes**, since every existing reader keeps reading the same `direction`/`stance` field name, now cushioned; only `collector.run()`'s axis-build call site changed (load state → confirm → merge → save). **D-A2 (first run / no persisted state): adopt the raw value as confirmed IMMEDIATELY** (streak seeded at 2) — no artificial deploy-day lag, and deliberately NOT a seeded fake "prior raw" from an old snapshot (that would flip a live book's reference on deploy day for a decision the hysteresis never actually made — rejected alternative, on record in the PR). The growth axis additionally gets `direction_change_diagnostics` (`collector._growth_rolloff_diagnostics` + `_load_prior_growth_axis`, reusing the Task-B-fixed walkback) whenever the raw classification flips: `head_vintage_dropped` + `newest_vintage_delta` + `attribution` (`"window_rolloff"` when the window slid by exactly one vintage AND the newest print's own delta doesn't support the flip direction — the 07-28 case; `"new_print"` otherwise; `"indeterminate"` with no prior snapshot). **D-A1 (policy stance in scope, default YES):** the market-implied DGS2-threshold stance gets the identical N=2 gate via the same `_confirm_axis_direction` helper — tracked independently of whichever layer governs `policy_axis.stance` on a given day (`raw_stance`, `stance_pending`, plus `confirmed_market_implied_stance`/`confirmed_market_implied_as_of` for what gets persisted) — but **a fresh manual `config/fomc-stance.json` bypasses confirmation and governs SAME-DAY** (an actual central-bank decision is a real print, not a windowed-series artifact — same doctrine as D-G2's "fresh authoritative input adopted immediately"). Prompt doctrine: when `direction_pending`/`stance_pending` is true, state "raw `{X}`, unconfirmed (`{raw_streak}` of 2) — reference still anchored `{confirmed}`"; on a confirmed flip, echo `direction_change_diagnostics.attribution` and never claim vintage windows are "the same" without comparing head-to-tail.

**`day_pl_zero_watch`** (session 2026-07-28, Task D — diagnostics only, no fix attempted) flags a held position whose reported day P/L printed exactly $0.00 while its total P/L moved by more than $25 (default) since the prior snapshot — a symptom, not a diagnosis (KMLM printed this three reports running, 07-24/07-27/07-28; USMV/AIA showed the same pattern briefly and healed). `collector._build_day_pl_zero_watch` (+ `_load_prior_position_total_pl`, reusing the Task-B-fixed walkback) echoes the RAW Alpaca position fields relevant to day-P/L derivation (`lastday_price`, `current_price`, `unrealized_intraday_pl` — the field our pipeline actually maps `positions[].day_gain` from — and `change_today`) so a future session can adjudicate upstream-Alpaca-bug vs. our-pipeline-mapping-bug; this session deliberately does not attempt that adjudication. The prompt surfaces any flagged rows verbatim under the Data Integrity Warning heading. **Resize awareness (session 2026-08-01, Task C):** the original diagnostic diffed `unrealized_pl` with no share-count normalization, so a genuine RESIZE (a sale realizing gains, a buy changing the lot basis) produced a large, fully-explained total-P/L delta that still read as an unadjudicable anomaly (07-31: KMLM's `-$93.87` was the 179-share sale; COWZ's `-$39.98` was partly the 18-share buy). `_load_prior_position_total_pl` now also returns a prior-quantity map (`portfolio.positions[].quantity`, same walkback, extended not duplicated); `_build_day_pl_zero_watch` takes an optional `prior_qty` map and suppresses a resized position from delta-only flagging UNLESS the independent price-identity signal (`lastday_price == current_price` — a literally frozen quote, the genuine separate anomaly from the 07-24/27/28 sessions) also fires, in which case it still flags, annotated `position_resized: true`. Omitting `prior_qty` (the default) preserves the original unconditional delta-only behavior exactly — no share-count data means no resize determination is possible.

**2026-07-21 audit blocks (entry #44 — flex reactivation + deferred findings 4–8).**
- **`flex_quadrant`** (A1, decision D1) — the quadrant the FLEX engine treats as in force, so an indeterminate `active_quadrant` no longer freezes the sleeve (it had since 2026-07-02). `collector._build_flex_quadrant` calls the pure `flex.regime.resolve_quadrant(g, i, bench_5d)`: both axes pinned → `(active_quadrant, "active")`; a 2-quadrant `favored_bucket` (e.g. Q3/Q4) → the member with the better trailing 5-trading-day `QUADRANT_BENCHMARK_ETF` return (`"borderline_5d_tiebreak"`); empty bucket or any missing benchmark return → `("", "unresolved")` (fail-closed, never guess). The 5d returns reuse the sleeve-scorecard closes cache (`_sleeve_selection_metrics(..., cache=)` — QQQ/XLI/GLD/TLT are all pool members) at **zero extra FMP calls**; window constant `_FLEX_TIEBREAK_WINDOW_D = 5`, deliberately not an env knob. The engine reads `snapshot["flex_quadrant"]["resolved"]`, falling back to strict `active_quadrant(axes)` when the block is absent/malformed (old snapshots). Exit logic unchanged (unknown quadrant never forces an exit); the basis propagates into the G1 `skip_reason` and `flex_state`.
- **`flex_separation_set` + `FLEX_REENTERABLE`** (A2, decision D3) — the retired fixed-24 `flex.regime.CORE_TICKERS` is deleted; `flex_separation_set(held)` derives the flex no-touch set from the LIVE `sleeve-roles.json` pools (every pool member of every role) + `LEGACY_EXITS` doctrine: non-re-enterable legacy (AMZN/GOOGL/DBA/TIP/XSD) always blocked; the re-enterable four `FLEX_REENTERABLE = {INTC, MCK, PPA, EUAD}` blocked only while HELD (a name mid-wind-down must not be in both books). `_flex_nominations` now takes the broker-held symbol set. Fixes the stale-universe bug (KMLM/XLV/IEF/… and half the flex-candidate seed were unenforced/un-nominatable).
- **B1 non-selected floors zeroed** (decision D2, completes PR #24 Option 1) — `_build_reference_weights` now zeroes every NON-selected pool member (SOXX/PAVE/XLB/GLDM/IAU/IHE/STIP/DBMF/CTA/SPLV/…) instead of leaving each at the 0.1%-of-core floor (~1.01% of equity of permanently-unfillable reference + a phantom `unclassified` bucket). The `selected` incumbent keeps its floor, so a future `selected` commit transfers floor+reference to the new incumbent automatically (mechanism follows the config, not the ticker).
- **`functional_coverage`** (B3, deferred finding 7) — deterministic Table-B (functional coverage, NOT additive to 100%): `collector._build_functional_coverage` counts each held name in EVERY quadrant its role covers (SGOV → Q4 primary + Q3 secondary; VDE → Q2+Q3), plus `excluded` (off-roster/legacy) and `sgov_note_inputs` (`sgov_pct`, `committed_q4_pct` = Q4-exclusive-role holdings). Echoed verbatim (the 07-20/21 Table B arithmetic was broken).
- **`freshness` + `growth_axis.as_of`** (B4) — `_build_growth_axis` now emits `as_of` = the realtime `asof` of the newest vintage row actually USED (vintage recency, not observation-quarter age — kills the GDPNow "3d↔81d" flip). New `freshness` block dates every tracked series deterministically with a cadence-aware threshold + `convention` (`observation_date` for macro — monthly CPI/PCE 45d / daily 5d — vs `vintage_date` for GDPNow); the prompt's Data Freshness table quotes it verbatim and never re-derives a date/staleness.
- **`performance.excess_attribution`** (B5, deferred finding 5) — `_excess_attribution` decomposes the vs-SPY excess into `cash_contribution_pp = avg_cash_weight × (r_cash − r_spy)` and `invested_contribution_pp = excess − cash_contribution` (exact residual) for the inception + 30d windows. The prompt must cite it for any excess attribution — the reflexive "cash drag" story is routinely sign-backwards (when SPY is negative, flat cash ADDS excess and the lag is entirely in the invested book).
- **Earnings-calendar universe filter** (B2, deferred finding 4) — `_filter_earnings_to_universe` clips the market-wide FMP calendar to held ∪ every role's `selected` ∪ flex candidates ∪ held legacy exits before writing `earnings_calendar`, so held names' confirmed dates surface (GOOGL 07-22 was being missed) and "no relevant earnings" is meaningful. No extra FMP calls.
- **Prompt C1–C5** — basis enums echoed verbatim (C1); the gap table renders every target∪held row incl. unheld targets like COWZ (C2); the cash ceiling is cited from `reference_weights`/`risk-limits` binding (shock-3 ⇒ 25%), never `recent_reports` (C3, deferred finding 6); prior stated next-session intents are adjudicated (C4); a tranche-min size-floor is not framed as total impossibility (C5).

**2026-08-06 signal-integrity audit (branch `fix/20260806-signal-integrity-audit`, findings B1–B7/M1–M2/O1–O4/R1 from the trailing 08-03/04/05 reports).** Every task below shipped with a test that FAILED against pre-fix `master` and passes after (empirical-verification doctrine — see the PR body for the full before/after list). B5 (growth-axis recency slope) and R2 (Risk Score sensitivity) were deferred by decision — see FOLLOWUPS.
- **B1/B2 — `market_implied_quadrant`'s 6 dead votes + count-based confidence.** `credit.hy_oas.trend_4w` was referenced by both `_build_leading_growth` and `_build_market_implied_quadrant` but `_build_bond_signals` never actually SET it (key-name mismatch) — the HY-OAS vote read null in both every session. `_build_market_implied_quadrant` also never received a `close_cache` for its copper_gold_ratio/XLY_XLP votes (it read the perf-series `closes` dict, which is CORE_ROSTER-only and never contains CPER/XLY) — structurally always null. Fixed both at the source (`bond_signals.hy_oas_trend_bp` config threshold; a shared `_ratio_20d_signal` helper now feeds both `_build_leading_growth` and `_build_market_implied_quadrant` identically, reusing the leading-growth close-cache fetch — zero extra FMP calls). Confidence is now gated on POPULATED VOTE COUNT (0–8) + axis-sign agreement (`divergence-config.json → market_implied_quadrant.confidence_min_populated`), replacing the old score-MAGNITUDE gate under which 2 correctly-wired votes (the basket-momentum pair) alone could swing confidence to `high` — observed 08-03/04/05, and cited as override-eligible evidence on 08-04.
- **B3 — `market_shock` news channel: z-score, not absolute count.** `total_hits >= 25/15` alone (no price corroboration) drove `shock_level` straight to 3/2 — a persistent news theme (Iran/Hormuz, 130–147 hits/day) pinned `shock_level` at 3 for weeks with the price channel benign (SPY up, VIX down), lifting the cash-sleeve ceiling to `shock3_ceiling` (25%) every session and suppressing the de-cash mandate. `_build_market_shock` now scores PRICE and NEWS as two INDEPENDENT channels (`price_level`, `news_level`) combined via `max()`. News scores a z-score of `news_hits_total` vs a trailing baseline (persisted to `market-shock/news-hits-history.json`; a conservative single-session floor applies before enough history accrues). **Symmetric benign-tape guard:** whenever `price_level <= 1` (no elevated/acute price signal), news-alone is capped at 2, or at 1 once the SAME dominant news category has persisted ≥10 sessions — level 3 now requires genuine price-channel corroboration. Per Jorge's decision: `tranche_pp_max` is untouched this cycle — the fix only lets the ceiling itself fall on a benign tape.
- **B4 — DXY dollar-proxy fallback boundary.** `_should_use_dollar_proxy` (new pure helper) fixes `dxy_stale > 5` → `dxy_stale is None or dxy_stale >= 5` — the old check was dark at exactly 5 days stale AND dark whenever DTWEXBGS returned zero usable observations at all (`dxy_stale` is `None` in that case, since `dxy_latest_date` is never set), leaving the DXY signal blind on both the primary and fallback paths simultaneously. The DTWEXBGS/DGS2/DFF deep-fetch depth (`_MACRO_DEEP_FETCH_DAYS = _ROTATION_WINDOW_DAYS + 30`) was audited and found already sufficient (90 obs ≥ the ~65 needed) — renamed to a coupled constant for traceability, not changed.
- **B6 — `reconcile`'s re-risk shortfall flags gated on a deployable envelope.** `required_move_today` is computed per sleeve up to `tranche_pp_max` each, so during a multi-sleeve de-cash program (e.g. 3 underweight amplifiers each "requiring" ~10pp) the sum routinely exceeded what the model could sanely deploy in one session — every sleeve flagged `non_compliant_flagged` even when the model deployed its full aggregate tranche pro-rata. Fixed with an aggregate re-risk envelope capped at the SAME `tranche_pp_max` but at the PORTFOLIO level, allocated pro-rata to each sleeve's own required-today share. A sleeve moving at least its pro-rata share is now `rationed_by_envelope` (new status, excluded from the report's "file an override" addendum) rather than `non_compliant_flagged`; a sleeve moving LESS than its share still flags (the 2026-06-30 pathology guard is unweakened — verified by test). De-risk synthesis, sells-before-buys, min-notional, gate, EXEMPT_HOLDS, turnover cap all unchanged.
- **B7 — `day_pl_zero_watch` gains an identity-based trigger independent of delta size.** The detector required BOTH `day_pl == 0` AND `abs(total_pl_delta) > threshold_usd` to flag, so a stuck feed on a SMALL position slipped through entirely (08-05 flagged COWZ, large delta, but not VDE — identical frozen-quote symptom, small delta). `_build_day_pl_zero_watch` now has two independent trigger paths: the original `delta_trigger`, and a new `identity_trigger` (`day_pl==0 AND lastday_price==current_price AND qty>0`) that fires regardless of delta magnitude or even its availability. Resize-suppression (Task C2) now applies ONLY to a delta-only qualifier — the identity path is never suppressed by a resize. When >1 position qualifies in a run, a new `multi_symbol_note` field collapses them into one snapshot-wide note instead of N per-ticker paragraphs (full detail stays in `flagged[]`).
- **M1 — `prior_overrides_pending`: surfacing a filed override's falsifier state on later runs.** OverrideHistory already persisted `falsifier`/`falsifier_date` verbatim (audited, no fix needed there) — the gap was that nothing ever read them back. New `collector._build_prior_overrides_pending` queries OverrideHistory for still-live filed overrides (accepted/downsized, not yet graded by Phase 5, filed within 60 days) and echoes each one's falsifier + a best-effort deterministic `falsifier_met` (`shared/overrides.py::evaluate_falsifier`, a pure regex parse of the `<axis> <direction> <N>+ <unit>` phrasing this system's falsifiers actually use, evaluated against the axis-confirmation `raw_direction`/`raw_streak` state D-2 already tracks; returns `None` — never a fabricated verdict — when the free-text falsifier doesn't parse) + the current axis state the evaluation used. New top-level snapshot key `prior_overrides_pending`; prompt mandates a held/released/expired adjudication per entry. Motivating incident: 08-04 filed a de-risk TLT hold with a dated falsifier; 08-05 sold TLT to the floor without ever adjudicating whether that falsifier had fired (it hadn't — inflation's `raw_streak` was 4, not the required 5+).
- **M2 — deterministic sub-min-notional damper-sell rule.** The identical sub-`min_notional` required move on TLT produced "hold" on 08-04 and "sell to the floor" on 08-05 with no override sheltering either day — a per-session coin flip. `reconcile()` now tags a re-risk damper-sell shortfall `sub_min_notional_action: "trim_to_floor"` whenever the dollar value of `required_move_today` alone is below `min_notional_usd` AND no override shelters the sleeve (`allowed <= 0`) — a FIXED rule (chose always-trim-to-floor, consistent with the pre-existing "size-floored ≠ impossible" doctrine), never per-session discretion.
- **O1 — non-binding breakeven bridge for the inflation axis.** `_build_inflation_axis` emits `bridge_direction` (off the already-fresh T5YIFR/T5YIE, reusing `divergence-config.json`'s existing breakeven-bp threshold) + `bridge_basis`/`bridge_delta_20d_bp`, a SECONDARY read for the 60–65d gap between monthly core CPI/PCE prints. Purely additive — `direction` stays governed exclusively by realized core.
- **O2 — fresher USO oil-proxy trend for the inflation-axis energy overlay.** FRED's DCOILWTICO/DCOILBRENTEU run 8–9d stale every session — on the exact channel that can flip the axis to "rising" on a hot headline. `_build_inflation_axis` now accepts an optional `oil_proxy_close_cache` (USO, fetched once via `_close_by_date`) and sources the 20-session oil trend from it when available (>20 trading days), falling back to FRED when it isn't. New fields `oil_trend_source`/`oil_proxy_20d_pct`/`oil_proxy_as_of`; the overlay RULE (price trend, never the news-shock level) is unchanged.
- **O3 — deterministic ADP leading-labor sub-signal.** `collector._build_labor_leading` (FRED `NPPTTL`) surfaces ADP private payrolls as a new `labor_signals.leading` sub-block (`delta_1m_k`, `delta_3m_avg_k`, `forward_softening_flag` off a config-driven gap, `risk-limits.json → labor_leading`) — a LEADING signal available before BLS PAYEMS, which NEVER overwrites the binding `payrolls`/`scorecard` fields (verified byte-identical with/without ADP data). Motivating incident: 08-05's ADP miss (+44K vs +70K consensus) was caught only because the analyzer happened to parse it out of the forex-news feed. The task's "else FMP economic-calendar" fallback (for when FRED itself lacks NPPTTL) is not implemented — FRED-only with a clean `available:false` degradation was judged sufficient this cycle.
- **O4 — hybrid band makes small-reference strategic sleeves enforceable.** `reconcile()` gates out-of-band status on `min(gap_band_pp, relative_band_frac * reference_pct)` whenever `reference_pct > 0` (new config `override_protocol.relative_band_frac`, default 0.5), instead of the flat 5pp `gap_band_pp` alone. Before this, `intl_broad`/VXUS (reference ~2.0%) sat at 0.14% every session, always inside the fixed 5pp band built for the much-larger core amplifiers — invisible, unenforceable, never funded, no matter how far it drifted below target. A zero-or-negative reference (LEGACY_EXITS) is unaffected; big amplifiers are unaffected too (0.5× their much-larger reference exceeds the absolute band, so it stays binding). Applied consistently to both the out-of-band gate and the required-move shelter subtraction. Audited the "`intl_broad` gated to 0 while the deployment gate is CLOSED" doctrine (`reference_weights` above) for conflict — none: that fires only on a closed gate; the observed VXUS symptom requires an OPEN gate, a different code path.
- **R1 — MU 10× ingestion guard + P&L realized/unrealized labeling.** `_quarantine_flex_price` gains a Gate 0 (`_correct_10x_ingestion_error`) applied BEFORE the quarantine backstop: when a flex-candidate price is a clean ~10× (or 1/10th) multiple of its OWN 52-week-range midpoint AND the corrected value lands back inside a sane band, the feed is corrected AT THE SOURCE (`prices[sym]["c"]` mutated in place, so every downstream consumer sees the fix) instead of silently re-quarantining the identical bad print every session forever (observed: MU printing ~10× its real price every session, never corrected). The quarantine remains the backstop for anything not cleanly correctable. Separately, `_build_pnl_decomposition` now labels every contributor `position_status: "open"|"closed"` (open = currently held, even at $0 unrealized) plus a per-bucket `has_open_position` flag — before this, a fully-closed off-roster position's historical realized loss (e.g. −$803) could be narrated as an ONGOING structural drag in the same breath as that bucket's current weight correctly reading 0.00%.

**Catalyst Sleeve Funnel (session 2026-08-10, branch `feat/20260810-catalyst-sleeve-funnel`) — candidate discovery + `catalyst_score` ranking, and the `regime_fit` demotion.** The Flex engine's entry pipeline was fully built but its funnel could only ever surface names the book already knew about. Three fixes, all upstream of the engine itself (`src/flex/entry.py`'s gates otherwise unchanged — the trail/2R scale-out/time-stop/sizing math is untouched):
- **`earnings_calendar_market`** — the market-wide FMP earnings-calendar rows NOT already covered by the book's own `earnings_calendar` (which is unchanged — still held ∪ every role's `selected` ∪ flex candidates ∪ held legacy exits) are no longer discarded outright. `collector._screen_earnings_market_rows` screens the additional rows to a plain-ticker-format proxy (the calendar row carries no volume/market-cap field for a true liquidity floor — that applies downstream, see `catalyst_screen` next) and caps them (`_EARNINGS_MARKET_CAP`, 40, nearest-dated first), emitting `{rows, cap, dropped_by_cap}`. Zero extra FMP calls — the rows were already fetched.
- **`catalyst_screen`** — new `src/collector/catalyst_screen.py` (pure functions, no I/O) scores a DISCOVERY universe (`catalyst_screen.discovery_symbols`: genuinely new names, sourced from `earnings_calendar_market` ∪ market-wide `congressional` trades — both already fetched at zero incremental cost, capped at `_CATALYST_DISCOVERY_CAP`=25) via a deterministic `catalyst_score` — the EQUAL-WEIGHTED mean of up to 6 components (`earnings_proximity`, `news_recency`, `news_tone`, `momentum`, `regime_fit_score`, `political_flow`), each independently `[0,1]` or absent. **Equal weights are deliberate, not a placeholder to fix later** — same doctrine as `sleeve_selection`'s momentum blend (FOLLOWUPS #23): with no point-in-time backtest harness, a hand-tuned coefficient is an unfalsifiable prior dressed as signal, and tuning is explicitly gated on graded outcome rows (FOLLOWUPS #58) that don't exist yet. **ABSENT-VS-ZERO is the composite's load-bearing rule:** a component with no underlying data (most concretely, no scheduled earnings date) DROPS OUT of the mean rather than scoring 0 — the fix for the failure mode where scoring absence as 0 would silently turn the sleeve into an earnings-only strategy, which the account holder explicitly rejected. A candidate needs ≥4 of 6 components available to be `rankable` (`MIN_COMPONENTS_RANKABLE`) — below that, it is never nominated regardless of how flattering its (necessarily thin) score looks. `_build_catalyst_screen` (the collector-side glue, `catalyst_screen.py` itself does zero I/O) fetches profile + `get_historical_price_light` per discovery candidate (2 FMP calls each, the only recurring cost this funnel adds), applies the hard screen (held / `flex_separation_set` / non-reenterable-legacy / liquidity-below-`FlexConfig.min_adv_usd` / insufficient price history — see the next paragraph), scores survivors, and ranks. The top `_CATALYST_TOP_N` (15) merge into `flex_candidates` with a new `source: "screened"` provenance (alongside the pre-existing `"static"`/`"dynamic"`), running through the same price-quarantine guard as any other candidate. **Data-availability constraint (verified, not assumed):** the integrated FMP `historical-price-eod/light` endpoint returns close+volume only, no high/low, so a literal ATR is not computable from it — the `momentum` component and the "price history present" hard filter (`insufficient_price_history`) are close-price-only, never named as an ATR check. `src/config/flex-candidates.json`'s static seed and the analyzer-emitted `watch_candidates` dynamic list (FOLLOWUPS #8) are both unchanged and still feed `flex_candidates` alongside the screened names.
- **`regime_fit` demoted from a hard entry veto to a scored/contextual input.** `src/flex/entry.py` no longer `_skip`s on a regime mismatch — liquidity, entry window, gap-vs-ADR, VWAP, and sizing all run regardless (a monthly-vintage macro quadrant has no business vetoing a 5-day catalyst trade — cadence mismatch). `regime_fit` (boolean) is still computed and surfaced in the output dict and `flex_state`, unchanged in meaning; the D1 (2026-07-21) no-read-quadrant fix is unaffected. New `flex.regime.regime_fit_score(sector, quadrant, basis)` gives `catalyst_score` a GRADED reading the boolean can't express: `1.0` fit under a pinned quadrant (`basis == "active"`), `0.6` fit under a lower-confidence tiebreak/single-favored resolution, `0.0` a real mismatch (recognized sector, wrong quadrant), `None` absent (unresolved quadrant or unrecognized sector — no regime read at all, not a negative one). **`flex_separation_set` is UNCHANGED and remains an absolute hard gate** (book-collision prevention — a flex order in a name that's a core pool member — never a regime opinion); this demotion does not touch it and the two must never be conflated.
- The flex-nomination prompt section (`src/config/project-instructions.md`) is rewritten accordingly: regime fit is framed as one weighed consideration ("a mismatch is a WEAKER thesis, not a disqualified one"), and the model is told to READ `catalyst_screen.ledger`/`.nominated`, never compute a score itself. New `tests/test_catalyst_prompt_sentinels.py` guards the doctrine sentences, mirroring `test_prompt_hygiene_sentinels.py`'s pattern.
- **Three decision gates shipped as defaults, not yet confirmed by the account holder** (PR body has the full framing): pre- vs post-earnings-print flex entries (recommend post-print only for v1); overnight gap protection for a position whose selection is now influenced by overnight-sourced signals (day / GTC / flatten-before-close); the discovery-fetch cap (25) / top-N (15) / news lookback (7 calendar days) sizing.
- `scripts/probe_fmp_tier.py` (Task C, committed but not yet run — no live FMP key in the authoring session) is the empirical check FOLLOWUPS #34 has been gated on since 2026-07-04; see that entry.

**Flex Sleeve Performance Ledger (session 2026-08-10, later same day, branch `feat/20260810-flex-performance-ledger`) — the canonical sleeve performance record.** Before this, `flex/ledger.py` was open-positions-only and every close/realization path (`time_stop`, `scale_out`, a broker stop fill via `reconcile_ledger`'s `exits_to_record`, an entry that never actually filled) dropped the trade's outcome with no price, no P&L, no record — `_record_trade_history` wrote an audit-trail TradeHistory row (`extra={}`) but nothing performance-grade. Two new blobs in the `flex-ledger` container, both owned by `src/flex/trades.py`:
- **`closed-trades.json`** (Task A) — append-only, one row per REALIZED trade (`trade_id`, `entry_price`/`date`, `fills[]` — a 2R scale-out means 2+ fills per trade — `exit_reason`, `pnl_usd`/`r_multiple` [**null, never fabricated, when any fill's price is unknown** — `pnl_unavailable_reason` names why], `holding_days` [reuses `flex.exit_state.trading_days_between`], and the `catalyst_score`/`score_components`/`nomination_thesis` the name carried at entry — the single highest-value field for the eventual #58 weight-tuning question). **ONE funnel every close path routes through: `flex/handler.py::_finalize_closed_trade`.** It never trusts what the engine submitted — it re-derives the trade's fills from `AlpacaClient.get_activities("FILL", after=entry_date)` (**reconciliation is broker truth**; `flex.trades.merge_broker_fills` treats the engine's own recorded fills as a floor the broker's own activity is reconciled against, not a ceiling — the LAST unaccounted-for broker sell gets the actual `exit_reason`, any earlier one is a broker-confirmed `scale_out` the engine's own record missed). A ledger row with **zero confirmed broker BUY fills since entry writes NO closed-trade record at all** — the OTO entry order never actually filled, so there is no real trade to record; recording one would fabricate an entry price for shares never bought. `trade_id` is generated ONCE at open (`flex.ledger.new_entry`) and carried unchanged through every fill/repair for the position's lifetime — unlike `order_ids`, which a stop replace DOES overwrite — and is the idempotency key `record_closed_trade` dedupes on (a re-entrant reconcile tick, which runs every ~15 min, must never double-count P&L). `_record_trade_history`'s `extra={}` is backfilled with price/proceeds/pnl/trade_id on every close path so the TradeHistory audit trail and the closed-trade ledger can never quietly disagree with each other.
- **`equity-series.json`** (Task C) — one row/trading day (`sleeve_notional_usd`, `unrealized_usd`, `cumulative_realized_usd`, `total_equity`, `open_positions`, `closed_trades_to_date`), upserted by date on EVERY in-hours tick of `run_flex_intraday` (decision gate: converges to "last successful tick of the day" without needing to detect which tick is actually last). Missing day → a gap, never interpolated.
- **`/api/performance`** (Task D, `web/api/function_app.py::_sleeve_series`/`_attach_sleeve_series`) emits per-point `sleeve_contribution_pp`/`sleeve_trade_count` **cumulative from the WINDOW START — deliberately NEVER normalized to a start=100 buy-and-hold index** like `portfolio_norm`/`spy_norm`/`quadrants`: the sleeve is intermittently deployed and flat much of the time, so indexing it the same way would render simply opening a position as a "return." Top-level `sleeve_closed_trade_count_total` is the ALL-TIME count (drives the N=30 sample-size gate regardless of window). An absent/malformed blob degrades to `sleeve_available: false` with the rest of the response byte-identical to before this feature existed.
- **`web/performance.html`/`.js`** (Task E) render it on a SEPARATE `#sleeveChart` canvas below the main chart (never a 7th dataset — different units), sharing the x-axis/window selector/`regimeBands` plugin. Color `#b8348f`, validated via the `dataviz` skill's `scripts/validate_palette.js` against every existing series color and the panel surface (contrast ≥3:1 and CVD ΔE clean-PASS ≥8 on every pair — worst case 9.5 vs Q1 green; normal-vision ΔE ≥15 on every pair). Below **N=30 closed trades** the line renders dashed+grey with a visible caption; no Sharpe/win-rate/skill statistic anywhere on this panel at any N (an intentional, permanent constraint — this system doesn't have the sample size to support one yet, and pretending otherwise would be worse than the noise itself).
- **Phase C horizon mismatch (Task F, FOLLOWUPS #61) — recorded, not fixed this cycle.** `_OUTCOME_HORIZONS=[30,60,90]`/`_HEADLINE_HORIZON=60` (`collector/handler.py`) grade a recommendation's outcome at fixed calendar horizons built for the core book's monthly cadence — wrong for a sleeve with a 5-day time stop, since a flex trade is closed and gone before its first outcome stamp even matures. Sleeve-appropriate grading (realized `r_multiple` at `closed_date`, not calendar drift) is FOLLOWUPS #61, gated on #23 + #58. Flex TradeHistory rows are already correctly `layer: "flex"` tagged (`_record_trade_history` hardcodes it) — audited this session, not changed.

**2026-08-14 audit (branch `feat/20260814-thematic-conviction-oil-fidelity`, decisions D-1
through D-5) — report-to-broker fidelity, oil signal correctness, transition_watch
hysteresis, thematic conviction overlay.** Triggered by the 08-11→08-14 reports: a
six-order "Final trade plan" collapsing to one submitted order with no Data Integrity
Warning; `transition_watch` activating on a stale FRED oil leg the axis itself had
already bypassed; the whipsaw that produced; and four sessions narrating a live Hormuz
energy-supply theme with zero path to a sized position. Every fix below shipped with a
test confirmed FAILING on pre-fix source (`git stash` isolation). Suite 1026→1126,
ruff clean.
- **The VDE whipsaw (motivating incident for Tasks B/C):**

  | Date | VDE reference % | Current | Action |
  |---|---|---|---|
  | 08-11 | 0.091% | 4.22% (25 sh) | Sold 24 shares @ $169.95 → 1-share floor |
  | 08-12 | **4.118%** | 0.17% | Report proposed **buy 18 shares** @ $171.55 |
  | 08-13 | 0.091% | 0.17% | In-band, no action |
  | 08-14 | 0.097% | 0.169% | `de_risk` override filed to shelter +0.072pp |

  The 08-12 spike was `transition_watch` activating for exactly one session on a stale
  FRED WTI reading while `inflation_axis` itself had already correctly preferred a fresh
  USO-proxy read (6.2%, below the 10.0pp trigger threshold) over the stale FRED leg
  (17.8%, above it) — the divergence detector (`_div_leading_vs_lagging_inflation`) was
  simply reading the wrong field (**decision D-2/Task B1**).
- **Decision D-1 (Task A, report-to-broker fidelity).** An A0 empirical probe (real blob
  reads from `stpfautoprod`, not inspection) found **Case 1 — LLM self-inconsistency, not
  validator rejection or executor drop:** the 08-12 model's own structured `trades[]` JSON
  never contained the 6 orders its markdown narrated — only a single, entirely
  band-enforcement-synthesized COWZ buy — and `daily-trades.json`/`daily-executions.json`
  matched each other exactly that day. This matters architecturally: `plan_vs_submitted`
  (A2, JSON-vs-broker fidelity) structurally CANNOT catch a prose-vs-JSON divergence,
  since it never reads the markdown; `analyzer._submitted_order_addendum` (A1, rendered
  directly from the final validated `trades[]`, placed immediately under the narrative)
  is what actually makes the divergence visually undeniable. Both are still valuable —
  A2 guards a genuine, different failure mode (a real executor bug or clamp) — but A1 is
  the fix that closes the OBSERVED incident. A3 hardens the prompt against the resulting
  08-13 false "executed as planned" reconciliation with an explicit anti-fabrication rule.
  A gap found DURING the A0 probe itself: `_split_response`'s "malformed JSON" branch
  never persisted a debug-raw copy (unlike its sibling "marker missing" branch) — App
  Insights retention had already expired by the time this incident needed forensic
  reconstruction, blocking a definitive parse-failure-vs-model-omission determination.
  Fixed alongside A1.
- **Decision D-2 (Task B, oil signal correctness).** `inflation_axis.oil_20d_pct_governing`
  (new field) is now the ONE oil reading for every purpose — `_div_leading_vs_lagging_inflation`
  reads it instead of the raw FRED leg directly, echoing whichever source
  (`oil_trend_source`) the axis itself already resolved. B2 adds real per-leg `as_of` +
  staleness gating to divergences 1/2/5 — the realized-core leg deliberately uses the
  freshness block's 45-day MONTHLY threshold (`_FRESHNESS_MONTHLY_THRESHOLD_D`), not the
  flat 7-day daily threshold the other legs use, since core CPI/PCE genuinely only
  refreshes monthly and a flat 7-day gate would make that leg permanently stale between
  prints — a deliberate deviation from a literal single-threshold reading, documented in
  the divergence function's own docstring. B3 requires the prompt to cite one governing
  oil number with source + as-of + the non-governing reading, every time.
- **Decision D-3 (Task C, transition_watch hysteresis).** Mirrors the existing
  axis-direction confirmation pattern (`_confirm_axis_direction`/`AxisDirectionState`) but
  needs two independent thresholds (confirm vs release, not just one) plus a fraction
  ramp, so it is its own function (`_confirm_transition_watch`) atop the UNCHANGED
  stateless `_build_transition_watch` evaluation, persisted via a new `TransitionWatchState`
  table (single row). A different candidate emerging while active does NOT count as a
  release miss — the old projection stays in force, unaffected, while the new one builds
  its own confirmation streak (never a no-projection limbo mid-transition); only a
  genuine absence (the stateless evaluation produces nothing at all) counts toward
  release. `staged_fraction` (the field every existing consumer already reads) becomes
  the RAMPED/applied value; a new `target_fraction` field carries the un-ramped target.
- **Decision D-4/D-5 (Task D, thematic conviction overlay) — the substantive architecture
  addition.** Jorge's decision: the LLM advises via an explicit probability-of-increase
  (`p_up`, "total return positive over `horizon_days`") rather than a mechanical
  oil-level trigger, and position size scales by confidence via a coarse ladder rather
  than being fixed. **Rejected alternative, on record: a mechanical trigger (e.g. "Brent
  > $90 for N sessions")** — rejected because a level-and-duration rule is exactly the
  kind of brittle, single-metric gate this whole audit cycle was fixing elsewhere (B1's
  stale-oil-leg bug, the 07-21 audit's mechanical-veto retirement for `regime_fit`); an
  LLM probability estimate, properly constrained, generalizes across themes the way a
  hard-coded price level never could.
  **Task D1's five mandatory safety properties** (an LLM-emitted probability directly
  sizing a position is an unbounded self-grading loop unless ALL five hold): (1)
  **quantized** — `_thematic_ladder_lookup` maps a continuous `p_up` to one of 5 coarse
  bands (`risk-limits.json → thematic_conviction.ladder`); a 0.63 and a 0.66 in the same
  band must produce the identical target — false precision jitters session to session.
  (2) **evidence-bound** — `min_evidence_items` (2) named deterministic snapshot-block
  citations; below the bar the nomination is REJECTED, never downsized (`insufficient_evidence`
  in `thematic_conviction.excluded`). (3) **bounded** — `per_ticker_cap_pct_of_equity`
  (4.0) + `aggregate_cap_pct_of_equity` (8.0), the latter scaled pro-rata across
  nominations, never truncated by rank. (4) **hysteretic** — per-symbol confirm/release
  (`_confirm_thematic_entry`, new `ThematicConvictionState` table, one row per nominated
  symbol) + a `max_session_delta_pp` (1.5) ramp, mirroring Task C's discipline exactly —
  without this the same VDE whipsaw would just move to a new lever. (5) **graded** — a
  new `ThematicHistory` table (PK=year-month) + `_stamp_thematic_outcomes` (mirrors
  `_stamp_switch_outcomes`'s per-horizon-independent pattern, reusing the SAME
  perf-series-closes-first/FMP-fallback price resolution) resolve `actual_up_{30,60,90}d`
  + excess vs SPY; `_build_thematic_calibration` computes Brier score + hit rate, feeding
  a `brier_damping` ladder that mechanically zeroes the lever (`damping_factor`) when the
  model's own track record is poor — below `brier_min_sample` (10), no damping (a thin
  sample proves nothing either way).
  **Eligibility (D4, `_thematic_classify_symbol`):** a `LEGACY_EXITS` name or a
  price-quarantined name is always excluded; a `CORE_ROSTER` name must be its role's
  EFFECTIVE selected incumbent (a non-selected pool member fights the existing D2
  non-selected-zeroing loop, producing a permanently unfillable reference — excluded with
  reason `non_selected_pool_member`, the correct remedy is a sleeve-selection switch, not
  a thematic lift, per **decision gate D-6**, FOLLOWUPS #64); a ticker not in
  `CORE_ROSTER` at all is never excluded — it routes to flex (`flex_source: "thematic"`,
  D4 rule 3, unchanged doctrine: new tickers enter only via flex).
  **Integration (`_build_reference_weights`) — a floor lift, never a ceiling:**
  `core_target[t] = max(core_target[t], thematic_target)`, budgeted from the
  non-active-quadrant remainder (the "living hedge" `active_quadrant_ceiling_pct_of_core`
  leaves inside the core block); when the lift exceeds that room, the active quadrant's
  own names are trimmed pro-rata and `thematic_lean.ceiling_pressure: true` surfaces with
  the exact pp taken — never a silent breach of the 90%-of-core ceiling.
  **Deliberate deviation from the literal D5 spec, verified empirically:** the spec frames
  this as inserted "before step 4's floor assembly" (i.e. against `raw_core`/`core_target`,
  which are %-of-CORE at that point) — but `thematic_conviction`'s targets are %-of-EQUITY
  by design (D2), and the %-of-core→%-of-equity conversion factor (`scale`, derived from
  `core_room`) is not computed until step 5. The lift is applied immediately after step
  5's equity-denominated `weights` dict instead — functionally identical, at the only
  point in the pipeline where the units actually line up (confirmed via a direct probe:
  a small lift leaves the active quadrant's own weights byte-identical; a large lift
  correctly trims them and flags `ceiling_pressure`).
  **Ladder numbers are a starting proposal, not yet reviewed** — flagged for Jorge,
  FOLLOWUPS #63.

**`quadrant_performance`** (FOLLOWUPS #12) answers the question the 2026-07-02 report exposed: the system rotated into Q3/Q4 while the Q3 basket was the worst performer since inception, and nothing forced the analyzer to engage with that tension. Built by `collector._build_quadrant_performance` right after the `performance` scoreboard so it reuses the SAME in-run perf series (`read_perf_series()` result, never re-downloaded). Per Q1-Q4 bucket (current `QUADRANT_CONCENTRATE` membership): `ret_30d_pct`/`ret_60d_pct`/`ret_90d_pct` + `excess_Nd_pp` vs SPY — window returns mirror `web/api/function_app.py::_quadrant_series` semantics (base = first close INSIDE the window slice, via a deliberate pure copy `_quadrant_perf_series` kept in lock-step by hand since the SWA API can't import this module) — plus a **hysteresis scan**: `favored_streak` (consecutive sessions ending today the bucket has been in that day's `favored_bucket`), `streak_excess_pp` (basket cumulative excess vs SPY over the streak, based at the session BEFORE the streak began), `lagging_sessions` (the current run of sessions, recomputed AS-OF each session — not just today's number — where that streak excess was negative), and `suspect` (true when the bucket is favored today AND `lagging_sessions >= suspect_after_sessions`, config `risk-limits.json → quadrant_performance`, default 10). Top level: `{as_of, spy_ret_30d_pct, buckets, favored_today, roster_note}` (`roster_note` flags that early-window returns under-represent the 2026-07-10 roster-revision's new members, same caveat as the `/performance` web chart). **Describe-only — never touches `reference_weights` or any deterministic gate**; the analyzer's prompt (Section 1 echo + a mandatory confronting paragraph when any favored bucket is `suspect`, raising the evidentiary bar for INCREASING that bucket's weight further) is the only consumer. A `regime_suspect` layer OverrideHistory row is written per suspect bucket per report day (analyzer `_write_regime_suspect_history`) so #13's monthly review can grade "what did we do when the market disagreed, and who was right" — **no stamper grades these rows yet** (`_stamp_override_outcomes` requires override-shaped `falsifier_date`/`sleeve`/`direction` fields via `_grade_override`; `_stamp_switch_outcomes` hardcodes an allow-list of `layer` values that excludes `regime_suspect`); a future grading pass would need a third stamping path comparing the favored bucket's forward return vs SPY from the flagged date, mirroring `_grade_switch`'s shape rather than `_grade_override`'s.

**`regional_rotation.rotation_score`** is a composite 0–10 (dollar 30 / relative-strength 30 / policy 20 / valuation 20) with a `category` (`us_leadership_intact` ≤3 / `transition_window` 4–6 / `rotation_underway` 7–10). The category is bucketed on the **rounded** `composite` (via the pure `_rotation_composite_category`), not the raw weighted score — so the displayed number and the label can never disagree (fixes the 2026-07-09 seam where an unrounded 3.049 displayed 3.0 but was labelled `transition_window`).

**`divergences`** (responsiveness brief Phase 2) is a deterministic list of **tensions between signals that should agree but don't** — it points the analyzer's judgment at high-value zones but **only describes; it never resolves, ranks, or acts** (resolution is the LLM's job in Phase 4). Built by `collector._build_divergences`, echoing values already in the snapshot. Four detectors: `leading_vs_lagging_inflation` (breakevens + oil vs realized core direction), `credit_complacency` (HY OAS at a ≤10th-pct-rank complacency extreme with no corroborating stress → `fragility`), `price_vs_regime` (SPY vs its 200-day SMA vs the deterministic `active_quadrant`), `dollar_vs_intl_tilt` (the DXY switch vs the book's aggregate amplifier-intl weight). Each entry is `{id, description, signals[], direction_implied, status}`; an input that is **stale or absent → `status: "indeterminate"`**, never a false `active` (missing data = WATCH, never REJECT). Thresholds in `src/config/divergence-config.json`. Two precomputed inputs feed it: a **SPY 200-day SMA** (reduced by the pure `_sma_from_rows` from already-fetched FMP rows; the fetch is in the orchestration layer so the detector stays no-network/testable) and the **aggregate international weight** (holdings × the `AMPLIFIER_INTL` roster). The Phase-4 prompt now adjudicates it (Section 2 divergence loop; an `active` one can be override evidence, an `indeterminate` one cannot).

**Override protocol + enforcement** (responsiveness brief Phase 4/Finding 2/#28 — the phases that **change report behavior and make the reference enforceable**). The analyzer prompt **consumes `reference_weights` / `divergences` / `transition_watch`** and executes toward the reference: Section 2 has a **Reference column** and **Current-vs-Reference gap**, and **Recommended = Reference ± logged overrides**. Deviating — *including a "hold" of a sleeve more than `gap_band_pp` off reference* — requires a structured **`overrides[]`** record (**OVERRIDE_SCHEMA_V1_1**, with a mandatory per-sleeve **`sleeve`** field): `{sleeve, premise_challenged, direction (de_risk|re_risk), magnitude_pp, evidence[], falsifier, falsifier_date, clean_data_only}`. `analyzer.assert_override_prompt_schema` gates the prompt on load (sentinels `OVERRIDE_SCHEMA_V1_1`/`reference_weights`/`overrides`), mirroring the flex gate. Three deterministic layers run post-model:
- **Tier-2 override validation** (`shared/overrides.py::validate_overrides`): structural gates (sleeve + falsifier + date + clean non-empty evidence + within `max_magnitude_pp` band + valid direction) reject; the **de-risk/re-risk asymmetry** (spec §6) accepts a de-risk override on one clean item, **downsizes** an under-evidenced re-risk override, **rejects** one with no evidence. **Direction is derived, not trusted** (session 2026-07-15, Task E1): `derive_override_direction` (`shared/reference_execution.py`, shares the block model with `is_de_risk_move`) computes the direction from the sleeve's block + the gap sign (current % − reference %) — a Damper/SGOV sleeve held ABOVE reference (or an Amplifier/`LEGACY_EXITS` sleeve held BELOW) is `de_risk`; the opposite is `re_risk`. The DERIVED direction — never the model's raw claim — governs the asymmetry bar; a disagreement is corrected-and-flagged (a reason is appended, the override is not rejected for it). Both the effective `direction` and the model's original `declared_direction` persist to OverrideHistory (2026-07-14 correctly filed a GLD-above-reference hold as `de_risk`; 2026-07-15 filed the identical case — plus XLP/TLT — as `re_risk`, backwards, which would have imposed the harder evidence bar on a cheap override, and would have inverted the sign `_grade_override`'s Phase-5 counterfactual grading uses).
- **Band enforcement** (Finding 2, `shared/reference_execution.py::reconcile`): D1 an override caps the *residual* not the move (`required_move_total = max(0, |gap| − max(allowed_residual, gap_band_pp))`); D2 tranche pace (`required_move_today = min(required_move_total, tranche_pp_max)`); D3 a de-risk shortfall (sell overweight amplifier **or a `LEGACY_EXITS` name** / buy underweight damper-or-SGOV — the `LEGACY_EXITS` sell leg added session 2026-07-15 Task D1, since slow-walking a wind-down exit is unambiguously de-risk) is **synthesized** as a `source:"band_enforcement"` trade — re-risk shortfalls are only flagged `non_compliant_flagged` (never auto-added), and (session 2026-07-15 Task D2) surfaced in a dedicated report addendum (`analyzer._flagged_sleeves_addendum`), not just the JSON. This kills the 2026-06-30 silent-hold pathology (the model could previously just not-trade an out-of-band sleeve). `reconcile`'s cash accounting counts an off-roster held name's sell proceeds toward `cash_avail` (Task B2) while still never targeting an off-roster name for synthesis, and — since session 2026-07-15 Task B1 — runs against the Tier-1 validator's pass-1 SURVIVORS rather than the model's raw trades, so a buy the validator will reject can never be mistaken for already-spent cash (see "Data flow — Phase 1" step 9 above). **Gap-row pricing is paper-canonical for held names** (session 2026-07-17, Task A): `_build_reference_gaps`'s `_price` helper prefers the paper-account position's `current_price` for any HELD symbol — the same price `current_pct` is itself computed from — falling back to the FMP EOD close only for an unheld reference target (no position price exists). Before this fix FMP could win outright for a held name, and any FMP-vs-paper price divergence over ~3% mixed a paper-priced `current_pct` with an FMP-priced V3 landing calculation and could phantom-clamp a legitimate full exit to a 1-share stub (2026-07-16 MU incident: a 5.9% divergence clamped 2→1 share; a 2.8% divergence the next day happened to slip under the validator's epsilon and pass — luck, not correctness). A permanent coherence invariant is pinned in `tests/test_price_basis_coherence.py`: for every gap row with `held_qty > 0`, `current_pct` must equal `held_qty * price / equity * 100` within rounding. **Decision D-B1 (session 2026-08-01, chose Option 1 of two):** `reconcile`'s `move_pp` computation now excludes the qualifying literal-cash→SGOV carve-out notional (`min(buy_notional, max(0, pre_trade_cash − literal_cash_buffer_usd))`, mirroring `shared/trade_validation.py`'s own SGOV exemption formula exactly) from scoring as the model having "traded AWAY" from reference on SGOV. Before this, every sanctioned carve-out sweep (the SGOV literal-cash carve-out described above) scored strongly negative on `model_move_pp` regardless of it being a pure cash-sleeve composition swap — a false alarm on the exact steady state the carve-out exists to enable, firing on every future sweep while SGOV sits above reference+band (07-31: gap +17.74pp, model_move_pp -7.01pp for a 69-share sweep). A SGOV buy beyond the carve-out budget still scores negative on the excess — the fix narrows the exemption to the sanctioned budget, it does not blanket-exempt SGOV. Option 2 (measuring the SGOV row at cash-sleeve level, SGOV + literal cash vs a sleeve-level target) was considered and rejected as more invasive — it would change the gap semantics the prompt/gap-table already documents per-name. **Review-round fix M1 (same day, PR #33 review):** the exclusion above applied regardless of gap direction, so a genuinely UNDERWEIGHT SGOV sweep (the realistic morning-after-a-big-sell-day state) had its `model_move_pp` zeroed out too, causing `reconcile` to mark the sleeve `enforced` and synthesize a redundant `band_enforcement` SGOV buy on top of a sweep the model already proposed and agreed with. Fixed: the exclusion now applies ONLY when the SGOV buy is the away-from-reference side (`toward == "sell"`, i.e. SGOV overweight) — an underweight SGOV buy (`toward == "buy"`) is genuine progress and is now credited in full, never excluded.
- **Tier-1 trade validator** (#28, `shared/trade_validation.py::validate_trades`, runs AFTER the reconcile merge): V1 gate/roster (no amplifier buy while gate closed — **except an equal-weight within-role substitution funded by selling the same role's old member, which is regime-neutral and passes**; core-only in `trades[]`; **any buy of a LEGACY_EXITS name is rejected** — core re-entry closed, flex only), **V1.5 selected-member-only buys** (roster_revision_2026-07 seam fix): every pool member of a role reads as an ordinary `CORE_ROSTER` name to V1/V3, so a BUY of a non-selected pool member (e.g. SOXX when `semis.selected = SMH`) would otherwise pass straight through — a near-zero reference row lands inside the ± band window, and with no gap row V3 used to skip the window entirely. V1.5 rejects any BUY where `role_of(symbol)` is not None and the symbol isn't that role's `selected` incumbent ("non-selected pool member — selection changes go through sleeve-roles.json"), **except** the `intl_leader` role, whose `selected` follows `intl_governance.leader_pick` automatically (Task F) — a buy of the *current* `leader_pick` (passed in via `quadrant_ctx["intl_leader_pick"]`, sourced from the snapshot's `intl_governance.leader_pick`) passes even before `sleeve-roles.json` is committed to match. SELLs of non-selected pool members are unaffected (substitutions, unwinds). V2 exemption (retired to a no-op — `EXEMPT_HOLDS=()`; machinery kept), V3 window (post-trade weight inside `reference ± max(residual, band)`, floor-protected — **legacy exits AND non-selected pool members bypass the floor so an exit sell reaches 0** (2026-07-13 audit finding 2, B0 decided: sell-to-zero — `_non_selected_pool_member()` mirrors V1.5's role/leader_pick logic exactly, so a held non-selected pool member like EWZ/VSS/IEMG/IDMO/EWJ is no longer a permanent 0.1%/1-share dust stub; the bypass is never keyed on `reference_pct == 0`, since a *selected* out-of-favor name can legitimately show ref 0 and still owes its floor; the sell-clamp math also floors at 0 rather than occasionally computing a negative share count); **a BUY with no matching gap row is now rejected outright** ("no reference row — cannot window the buy") rather than silently skipping the window — closes the second half of the same seam; a SELL of a held name without a row keeps its prior behavior; an **off-roster held name** (a flex leftover like MU, 2026-07-13 audit finding 3) gets a `reference_pct: 0.0, off_roster: True` gap row from `_build_reference_gaps` so its SELL is still V3/V4-clamped — off_roster rows are filtered out of `reconcile`'s working set so band enforcement never synthesizes a trade against one, and `_post_validation_cash` also falls back to the paper-account position price when no gap row exists) — **with a SGOV literal-cash carve-out**: a SGOV BUY funded purely from *pre-trade* literal cash (not same-day sell proceeds) above the `literal_cash_target_pct` buffer is a pure cash-sleeve composition swap (cash sleeve = SGOV + literal cash), so it is exempt from the per-name window and clamped to the buffer budget rather than rejected (fixes the 2026-07-09 idle-cash pathology where SGOV at 28.44% vs a 28.50% window ceiling rejected a $4k cash→SGOV conversion); SGOV sells are windowed normally — V4 mechanical (sell ≤ held, buy ≤ cash-after-sells, integer shares, min-notional). Rejected trades move to `rejected[]` (never submitted); clamped trades trimmed to the window edge. **This is what makes "enforced downstream" true** — before #28 a hallucinated trade flowed from LLM JSON straight to Alpaca.

An override can never loosen the deployment gate or breach a Tier-1 limit (floor/ceiling/exempt). Override decisions persist write-once to **`OverrideHistory`** (`analyzer._write_override_history`, layer-tagged, PK=year-month); **Phase 5 (merged) stamps their matured outcomes** vs the reference counterfactual. Config: `src/config/risk-limits.json` → `override_protocol` (`max_magnitude_pp` 15, `re_risk_min_evidence` 2, `gap_band_pp` 5) + `reference_execution` (`tranche_pp_max` 10, `enforce` true, `enforcement_turnover_max_pct` 20, `min_notional_usd` 115). **Auto-execute is paper-only (`AUTO_EXECUTE_ENABLED=true`); a `validation_error` file is refused by the auto-executor (fail-closed); live trading still requires human approval.**

**Decision D-D1 (session 2026-08-01, chose Option (c) of three — prompt-only) — the cash-sleeve deployment deadlock.** D1's own "in-band gaps generate no obligation" rule, combined with the D3 asymmetry (a de-risk shortfall on Q1–Q4 sleeves is only synthesizable when they're OUT of band, and the cash sleeve's own corrective — selling a damper to fund a buy elsewhere — is re-risk and NEVER synthesized), produces a structural equilibrium: every Q1–Q4 sleeve sits sheltered at reference-minus-band with no obligation to trade, while the cash sleeve floats arbitrarily far above its operative ceiling with nothing in the deterministic layers able to correct it. Observed 2026-07-29→07-31: cash sleeve 37.19% → 48.42% → 54.87% (55.02% post-trade) against a 25% shock-3 ceiling, with the 07-30→07-31 SPY rally alone collapsing inception excess from +1.972pp to +0.328pp in one session. Fixed at the **prompt layer only** (no enforcement-layer change): `project-instructions.md`'s Cash sleeve section now states that when the cash sleeve sits above its operative ceiling, an in-band-but-underweight Q1–Q4 sleeve becomes **discretionarily** buy-eligible toward (never past) `reference + gap_band_pp`, funded from the excess cash — extending the existing "size-floored ≠ impossible" discretionary-move doctrine (previously scoped only to out-of-band sleeves) to this condition; paced to one tranche's worth at the cash-sleeve level per session; gate rules still bind per name. This is a **permission, not an obligation** — an in-band gap still requires no override and generates no enforcement synthesis if declined. Option (a) (extend D3 synthesis with a cash-sleeve-level trigger) and Option (b) (shrink `gap_band_pp` from 5.0 to 2–3) were considered and deferred: (a) touches the locked D3 asymmetry decision record above and would need its own decision entry if chosen later; (b) mechanically closes the shelter but raises churn across every sleeve, not just cash. FOLLOWUPS #50 has the full incident writeup; revisit after ~5 sessions if the prompt-only fix proves insufficient. **Review-round fix M2 (same day, PR #33 review):** the trigger as originally worded — "`cash_sleeve_target_pct`'s actual value sits above its operative ceiling" — could never fire: `cash_sleeve_target_pct` is `max(cash_floor, min(cash_ceiling, cur_sleeve))` (`collector._build_reference_weights`), ceiling-clamped BY CONSTRUCTION, so it can never itself exceed the ceiling; `functional_coverage.sgov_note_inputs.sgov_pct` (the other figure cited) is SGOV-only, not the sleeve total. Reworded to key on the deterministic `reference_weights.binding` list containing `"cash_above_band"` (which the collector already appends whenever `cur_sleeve > cash_ceiling`, `cur_sleeve` being the live literal-cash + SGOV total) — or the current cash-sleeve figure read directly (`quadrant_allocation.buckets.cash_sleeve`) — never on `cash_sleeve_target_pct`.

**`transition_watch`** (responsiveness brief Phase 3) is a deterministic **pre-staging** signal: realized inflation is laggy, so when the Phase-2 `leading_vs_lagging_inflation` divergence is `active`, `collector._build_transition_watch` projects the quadrant the *leading* signal points to and emits a **bounded, partial lean** that `reference_weights` applies — **without moving the binding `active_quadrant` / `regime_gate` / realized inflation axis** (spec §6). It **reuses** the Phase-2 divergence (never re-derives breakevens/oil). Output: `{active, projected_quadrant, direction (de_risk|re_risk), staged_fraction, basis[], status}`. **Asymmetry (the safety):** a *de-risk* transition (projecting a more defensive quadrant) stages readily at the full fraction; a *re-risk* transition (more offensive) needs ≥`re_risk_min_confirmations` leading signals agreeing (breakevens AND oil) and a smaller fraction, else it does not activate. Leading data stale/absent → `status:"indeterminate"`, no lean. `reference_weights` applies it as a **convex blend** (`(1−f)·base + f·projected`, `f`≤0.30 — never a full flip) and surfaces it in its `transition_lean` field. Config in `risk-limits.json` → `transition_watch`. **Phase 3 changes `reference_weights`** but is still report-inert until Phase 4.

**`reference_weights`** (strategy-spec §10 — "precomputed target weights the LLM executes toward") is the deterministic per-ticker REFERENCE allocation the analyzer reasons *against* — the layer that anchors the call→target→trades leap where the book previously rationalized silent inaction. Built by `collector._build_reference_weights` from the active quadrant × a deterministic **conviction proxy** (0–10, stands in for the analyzer's Risk Score, which isn't available at collect time) × the **DXY dollar switch**, constrained by `src/config/risk-limits.json` (90%-of-core ceiling, 0.1% sleeve floor, single-name caps, cash band 5–15%/shock-3 25%). **Roster revision v2 (roster_revision_2026-07):** the core is now ROLE-BASED (`src/config/sleeve-roles.json` — a `selected` incumbent per role, resolved by `shared/quadrants.py`); the **AMZN/GOOGL exempt-hold doctrine is RETIRED** (`EXEMPT_HOLDS=()`, a no-op) and both are **LEGACY_EXITS** (reference target 0, liquidated in tranches, never re-bought into core; QQQ retains the mega-cap exposure). The **international sleeve is carved out separately** (like cash) and sized by `intl_governance` (rotation + DXY), NOT the US quadrant. **`intl_broad`'s base target is gated to 0 while the deployment gate is closed** (session 2026-07-15, Task C, decision C0 = Option 1): `intl_broad` is unconditionally `block: amplifier_intl`, so its buy is Tier-1-rejected on every closed-gate day regardless of the rotation score — confirmed rejected 2026-07-14 AND 2026-07-15 ("amplifier buy VXUS forbidden"), which wasted a trade slot daily and (pre-Task-B) starved enforcement cash. The gating is self-healing (rebuilt every day, restores automatically the day the gate opens) and doesn't force-sell a held position (the resulting gap sits inside the override band). The `intl_leader` slot is untouched by this — it stays rotation-governed, already halved (never zeroed) on a closed gate per roster_revision_2026-07 §4. It also applies the Phase-3 **`transition_watch`** lean as a bounded convex blend (surfaced in `transition_lean`). Emits `target_weights_pct` (per-ticker % of equity), `by_quadrant` (deterministic aggregation, incl. an `intl` bucket), the conviction proxy + drivers, `active_quadrant`/`favored_bucket`/`borderline`, `dollar_tilt`, `transition_lean`, and `binding` constraints. It is a **reference, not a mandate** — the analyzer may deviate only via a falsifiable, magnitude-bounded, logged override (Phase 4), and any gap it neither trades nor overrides is force-closed at tranche pace by Finding-2 band enforcement.

**`sleeve_selection`** (roster_revision_2026-07, Task E; blanket autonomy, session 2026-07-27) ranks each scorecard role's candidate pool: a momentum blend (0.5·r120 + 0.3·r60 + 0.2·r252, renormalized over available windows) − a static expense-ratio penalty, with a 120d benchmark-correlation eligibility floor (<0.6 = ineligible). A `switch_signal` fires only under hysteresis (a challenger leading by ≥2.0 for ≥10 consecutive runs; streak in the `SleeveSelectionState` table) — and, since 2026-07-27, **a `switch_signal` on an UNPINNED role auto-advances the EFFECTIVE incumbent** (`SleeveSelectionState.selected`), mirroring the pre-existing `intl_leader` auto-rotation pattern; no human config commit is required for it to take effect. Per-role output now carries `{incumbent, config_selected, effective_selected, scores, ineligible, challenger, lead, streak, switch_signal, auto_switched, pinned}`. The analyzer echoes it (never decides) — see "Sleeve auto-switch" below for the full doctrine.

**`role_selection`** (session 2026-07-17, Task C; extended 2026-07-27) fixes a gap `sleeve_selection` never covered: it only ranks `selection: "scorecard"` roles, so `intl_leader` (`selection: "rotation"`) never appears there at all. `collector._build_role_selection` echoes EVERY role's `{role_id, selected, effective_selected, selection}`, with `intl_leader`'s entry additionally carrying the current runtime `leader_pick` (from `intl_governance`) plus a note that `selected` (config) changes ONLY via a committed `sleeve-roles.json` edit while `effective_selected` (live) may already differ from it via auto-switch — two independent axes a 2026-07-17 report conflated (the model proposed selling AIA's 1-share floor because `leader_pick` went null; Tier-1 correctly rejected it via the pre-existing `_non_selected_pool_member` floor-bypass design). The floor guarantee follows `effective_selected`, not `selected`.

**Sleeve auto-switch (blanket autonomy, session 2026-07-27, Jorge's decision).** Scorecard role switches are now **fully autonomous**, mirroring the existing `intl_leader` rotation pattern (V1.5 already had a runtime-pick exception for that one role): every `switch_signal` on an unpinned role fires — **blanket, no size gate** (Jorge's decision; `pin` is the only per-role brake). `sleeve-roles.json`'s `selected` becomes the **baseline/pin**, not the live authority; the live authority is `SleeveSelectionState.selected` (the **effective incumbent**), computed by `shared/quadrants.py`'s override-aware helpers (`concentrate_names`, `selected_for_role`, `is_amplifier`, `primary_quadrant`, `quadrant_allocation_bucket`, `selected_core_members`, `amplifier_set` — each takes an optional `overrides`/role→ticker map; omitted/empty reproduces the frozen zero-arg config read exactly, so every pre-existing caller is unaffected).
- **`"pin": true`** (per role, `sleeve-roles.json`) disables auto-switch for that role entirely — config wins every run, exactly like the pre-2026-07-27 doctrine.
- **D-G1 (old incumbent's end state): full exit to zero**, i.e. exact parity with what a committed `selected` edit already did — the D2 non-selected-pool-member zeroing loop and the V3 floor-bypass (`_non_selected_pool_member`) both key on the EFFECTIVE selected set, so the deselected old incumbent (e.g. XLV after `healthcare_def` flips to IHE) loses its floor and is sold to zero via band enforcement, while the new effective incumbent inherits the floor + reference target.
- **D-G2 (config-edit adoption): an unpinned role's config `selected` change is adopted as freshly authoritative next run**, never silently shadowed by an in-flight auto-switch — `SleeveSelectionState` persists a `config_selected` column; a mismatch against the role's current config `selected` triggers immediate adoption (streak/challenger reset).
- **A challenger inside `LEGACY_EXITS` (e.g. `semis`' XSD) is never adopted** — core re-entry is closed to those names, so adopting one would create a structurally unfillable reference target.
- **Consumer inventory** (everywhere the effective map must resolve, or a switch ships with a visible day-one defect): `collector._build_reference_weights` (concentrate/borderline-intersection materialization, the Q1/Q2 US-vs-intl amplifier split, the `no_read_ballast.ballast_names` substitution — a name statically listed in `risk-limits.json` is remapped through the live incumbent before routing weight, else a future ballast-role auto-switch would route weight to a name the D2 loop is about to zero), the D2 zeroing loop itself, `_aggregate_by_quadrant` + `collector._build_quadrant_allocation` + the analyzer's post-trade `_quadrant_allocation_addendum` (Table A bucketing), `collector._build_price_universe` + the earnings-calendar universe filter (so a freshly auto-switched incumbent is priced/dated the SAME run it switches), and `shared/trade_validation.py`'s V1 amplifier gate (`amplifier_set`), V1.5 selected-member check, and `_non_selected_pool_member`'s floor bypass. `QUADRANT_BENCHMARK_ETF` is deliberately left static (flex G3 opportunity-cost pricing, not a holding).
- **Persisted-state ordering (failure isolation):** the collector loads `SleeveSelectionState` and derives `effective_selected` EARLY (before the price/earnings universe and `reference_weights`), then re-derives it from the scorecard's fresh decision on success — a scorecard-build failure (e.g. an FMP hiccup) falls back to the plain persisted-state read rather than losing every role's override for the day (no config/effective whipsaw).
- **Top-level `effective_selected` snapshot key (session 2026-07-28, Task E hardening).** The collector now writes the FINAL value of its `effective_selected` variable as a top-level snapshot key (populated even on a scorecard-build-failure day, per the failure-isolation bullet above). `analyzer._snapshot_effective_selected` prefers this key over scanning `sleeve_selection.roles[]` — the block that is exactly UNAVAILABLE on a failure day — falling back to the roles-scan only for an old snapshot shape (predating this key), then `{}`. Closes a seam found in the #47 merge audit: without this, a scorecard-build failure the day after a switch would have the reference still correctly targeting the new effective incumbent (per the bullet above) while the Tier-1 validator's ctx fell back to config — rejecting a buy of the effective incumbent and reinstating the deselected old incumbent's floor, for that one day.
- **Logging/grading:** an auto-advance fires the SAME `sleeve_switch` OverrideHistory row (`_build_sleeve_switch_records`) and `_stamp_switch_outcomes` Phase-C grading a human config commit always has — no new logging path was needed.

**`intl_governance`** (roster_revision_2026-07 §4, Task F — resolves FOLLOWUPS #36) governs the international sleeve by the **rotation score + DXY switch**, leader-selective: a small `intl_broad` base (VXUS) + a rotation-sized `intl_leader` slot (`leader_pick`). Sizing ladder off the rotation composite (≤3 base only; 4–6 base+1pp; ≥7 base+3pp, up to 2 leaders), then the DXY anti-chase (headwind→0, neutral→halve) and the **gate modifier (CLOSED → HALVE the leader tilt, never zero — this REPLACED the interim suppress-to-zero rule)**. De-rotation unwinds the leader on `composite_fade`/`leader_lost_status`/`ma_bearish`. The `intl_leader` slot follows `leader_pick` automatically (the one auto-selected role, logged to `OverrideHistory` layer `intl_leader_rotation` for Phase C grading vs the incumbent). `reference_weights` consumes it for the two intl roles.

## International sleeve (rotation-governed)
The intl sleeve is the `intl_broad` (VXUS; pool VXUS/ACWX/IXUS) + `intl_leader` (AIA; pool AIA/EWJ/IEMG/IDMO/VSS/EWZ) roles, sized by `intl_governance` (above), NOT the US quadrant. Need: ETF look-through from FMP, international macro from FRED (EUR/USD, USD/JPY, USD/CNY, ECB rate, China PMI, Japan 10Y).

## Rules
- Never store secrets in code — always Key Vault with Managed Identity
- All functions emit custom metrics to App Insights
- Blob is source of truth; tables can be rebuilt from blobs (the rebuild procedure is not yet written up as a standalone runbook — there is no `docs/runbooks/` dir; the reconstruction logic lives in the collector's table-write path)
- Human approval required for ALL **live** trade execution — no autonomous live trading. **Paper-only auto-execute** is enabled via app setting `AUTO_EXECUTE_ENABLED=true`: a 09:35 ET timer (`auto_executor`, NCRONTAB `0 35 9 * * 1-5`, ET-local via `TZ=America/New_York`) reads `daily-trades/{ET-today}.json` and submits every recommendation to Alpaca paper, gated by Alpaca market clock (defers if closed). **Retry timers** at 10:05 + 11:05 ET (`auto_executor_retry`, #29) cover variable analyzer latency / missed opens; idempotent via a cache asymmetry (terminal outcomes cached, failures not) + date-scoped `client_order_id`; a `no_trades` outcome at ≥11:00 ET logs ERROR (analyzer never produced the file). The executor **refuses** any file the Tier-1 validator flagged (`validation_error` or a rejected-stamped trade — fail-closed). It also applies a **defensive sell filter**: any sell against a symbol not held in the Alpaca paper account is dropped (status `not_held_in_paper_account`); oversize qty is trimmed. Drops recorded in `daily-executions/{date}.json` `skipped[]`.
- **One-time paper seeder** (`POST /api/seeder`): idempotent mirror of `src/config/portfolio.json` into Alpaca paper. Use `whole_shares_only=true` for off-hours seeding (Alpaca rejects fractional day-orders queued outside market hours — we floor qty to int, drop sub-1 tickers). Symbols already held OR with a pending open buy order are skipped (`skipped_already_held` / `skipped_open_order_pending`) so re-runs never duplicate. Per-run report written to `seeding/{utc_ts}.json`.
- Phase 1 must run clean 30+ days before Phase 2 is enabled
- Temperature 0.2 for Claude analysis calls (consistency)
- Sells execute before buys in multi-trade recommendations (free up cash)
- **Test-count baselines are MEASURED, never carried forward from a prior FOLLOWUPS
  entry or PR body.** Both the "before" and "after" numbers in a session's `Suite
  X→Y green` line come from an actual run — `git checkout <base-ref> && PYTHONPATH=src
  pytest -q | tail -1`, then the same on the branch — and both commands' output goes in
  the PR body. Diagnosed 2026-08-10 (PR #37 pre-merge correction): entry #57 recorded
  `922→979` for PR #36 without re-running the suite at the PR's actual base
  (`e402bbc`, the true count was 983); entry #60 then copied that already-wrong `979`
  forward as ITS baseline instead of measuring `e402bbc` itself, compounding one wrong
  number into two. A baseline copied from a written record — even one from the same
  repo, even from last session — is exactly the kind of unverified claim the
  empirical-verification doctrine (grep/inspection vs. actually running the code)
  already exists to catch; it just hadn't been applied to this specific number before.

## Deployment lessons (hard-won — see infra/modules/storage-roles.bicep + .github/workflows/deploy-code.yml)
- Function App MI requires **Storage Account Contributor** on the storage account in addition to Blob Data Owner / Queue Data Contributor / Table Data Contributor. Host startup calls `BlobServiceClient.GetPropertiesAsync()` which needs `blobServices/read`, not in the data-plane roles. Without it: persistent `AuthorizationPermissionMismatch 403`, host faults, zero functions registered.
- Workflow pip install MUST pin manylinux2014 wheels (`--platform manylinux2014_x86_64 --python-version 3.11 --implementation cp --only-binary=:all:`). GitHub `ubuntu-latest` ships GLIBC 2.39; Functions Linux Consumption image is older. Native wheels (e.g. `cryptography`) otherwise fail with `GLIBC_2.33 not found` and the Python worker silently fails to load.
- Deploy model: run-from-package via blob (`WEBSITE_RUN_FROM_PACKAGE=<blob URL>`). `func azure functionapp publish` does not work with identity-based `AzureWebJobsStorage`. App runs in read-only mode — portal Test/Run hits CORS; invoke via admin REST instead: `POST https://func-pfauto.azurewebsites.net/admin/functions/<name>` with master key from `az functionapp keys list`.
- Workflow path filter is `src/**`; workflow-only changes need manual `gh workflow run "Deploy function code" --ref master`.
- Quiver `/beta/live/lobbying` and `/beta/live/gov_contracts` return ~20K rows of all-market activity per call (~12 MB + ~4 MB JSON). Collector MUST filter client-side to portfolio tickers ∪ ETF watchlist AND last 90 days — otherwise the daily snapshot balloons to ~20 MB and the analyzer prompt blows past Claude's 1 M-token context window (observed: 6.2 M tokens → permanent 400). See `_row_ticker`/`_row_date` filter in `src/collector/handler.py` after the Quiver fetch. Commit `20cb2b0`.
- `host.json` `functionTimeout` is `00:40:00`. The old `00:10:00` was the **Consumption** plan max; on **Flex Consumption** the cap is far higher and the Foundry client allows up to 600 s/attempt × 3 retries, so 10 min could hard-kill the analyzer mid-Claude-call (no `Executed`, no exception, no report — just a silent recycle at the 10-min boundary). Large June snapshots (~1.2 MB / ~240K tokens) exposed this. Can also be overridden live without redeploy via the `AzureFunctionsJobHost__functionTimeout` app setting. Commits `743b5ad` (original), 2026-06-09 fix.
- **EventGrid blob-trigger `functionName` (Flex Consumption)**: the webhook URL in `deploy-code.yml` MUST use `functionName=Host.Functions.analyzer`, NOT bare `functionName=analyzer`. The bare form is accepted by `/runtime/webhooks/blobs` (HTTP 200, EventGrid reports DeliverySuccess) but cannot be routed to the registered listener, so the analyzer **never fires** and nothing errors — looks healthy end-to-end while silently dropping every event. Also requires the `Microsoft.EventGrid` resource provider to be **`Registered`** (not `Registering`) or storage publishes zero events (PublishSuccessCount=0). Diagnosed 2026-06-09.
- Runtime knobs `TZ=America/New_York`, `AUTO_EXECUTE_ENABLED=true`, and `AzureFunctionsJobHost__functionTimeout=00:40:00` now live **in `functionapp.bicep`** (IaC), so an `infra/**` deploy sets rather than wipes them. They were previously applied post-deploy, and an `az deployment group create` replaces the function app's app-setting set wholesale — which silently dropped them after the Flex migration and disabled the pipeline. Without `TZ`, crons run in UTC (collector 09:00 UTC = 05:00 ET; auto_executor 09:35 UTC = pre-market → defers forever). `AUTO_EXECUTE_ENABLED=true` in IaC intentionally overrides the "Phase 1 clean 30+ days" gate. Note: setting any app setting restarts the host — which is also what (re)connects Application Insights if telemetry has gone silent.
- **Editing `deploy-infra.yml` or `deploy-web.yml` self-triggers that deploy.** Both include their **own filename** in the push path filter (e.g. `paths: ['infra/**', '.github/workflows/deploy-infra.yml']`), so a workflow-only edit to either fires a full deploy on push. `deploy-code.yml` is the exception — it filters on `src/**` only, so editing it does NOT trigger (needs manual `gh workflow run`). Consequence observed 2026-06-15: bumping action versions in all three YAMLs triggered an unintended infra + web deploy — the infra deploy re-applied unchanged Bicep and, per the SWA secret-wipe issue, wiped the SWA's `STORAGE_CONNECTION_STRING` + `FUNC_MASTER_KEY`. **This whole failure mode is OBSOLETE as of 2026-07-11 (FOLLOWUPS #2, `fix/swa-hardening`)** — those two settings are now resolved from Key Vault at deploy time (`main.bicep`'s `getSecret()` calls), so an infra deploy sets the correct current value instead of wiping it; there is no more manual post-deploy runbook step. Also note: portfolio resources live in the **EasyGridsProduction** subscription under the **jgarrote@easygrids.com** identity — a DIFFERENT Entra tenant from a Quirch-default az session. With the wrong identity even `--auth-mode login` blob reads fail with `InvalidAuthenticationInfo: Issuer validation failed` (observed 2026-07-07). The working sequence: `az login --use-device-code` as jgarrote@easygrids.com, then `az account set --subscription EasyGridsProduction`. Only then do blob reads, `az functionapp keys list`, and KV secret reads succeed.
- **A stale resting Alpaca order permanently blocks all future orders on that symbol, invisibly.** Diagnosed 2026-07-15 (FOLLOWUPS #42, Task A0): a GTC stop order the flex engine placed on MU 2026-07-08 (as part of its own stop management, never sent through `_place_one` — core trades never carry resting stops) was left open (`status: "new"`) after the engine's internal `held[]` state dropped the position, locking both shares as order collateral (`qty_available: 0`). Every subsequent daily-executor sell attempt for MU 403'd — silently, since `_place_one` only logged the error and moved on, and nothing read `daily-executions/{date}.json` back the next day to notice. Confirmed via `GET /v2/positions/{symbol}` (`qty_available`) and `GET /v2/orders?symbols={symbol}&status=all`. **Fixed (collision point, 2026-07-15):** the executor now calls `_cancel_conflicting_orders` before every submission (cancels any pre-existing open order on that symbol first — today's recommendation supersedes a stale resting one); `execution_review` (Task A1) makes a recurrence visible in the next day's snapshot instead of silent. **Fixed (root cause, session 2026-07-17, Task F):** the flex engine itself now prevents the orphan from ever accumulating — `flex/reconcile.py::reconcile_ledger` returns a new `orphan_orders` list (every broker OPEN order for a symbol the ledger doesn't track at all, surfaced in `flex_state.reconcile.orphan_orders`), and `flex/handler.py::_sweep_orphan_orders` cancels its own orphans every ~15-min tick — STRICTLY scoped to its own `client_order_id` family (current `FLEXC-` + the legacy pre-split `flex-` naming), never a DayTrade Lab (`FLEXD-`) or daily-executor order. Additionally (**decision G1 = yes**), repair/trail stop orders switched `time_in_force` from `"gtc"` to `"day"` — a stop only triggers during regular hours anyway, so DAY gives identical protection with no immortal stale order surviving a future ledger-row loss; it is simply re-placed every in-hours tick by the existing no-naked-long check in `reconcile_ledger`.

## Spec documents (in docs/specs/ — each has a paired .docx generated by scripts/docx_to_md.py; the .md is authoritative)
Full details in these companion documents — read them for implementation specifics:
- **`growth_strategy_spec_v1.md` — the north-star strategy spec (regime-concentration machine; the source of truth all automation is downstream of). Read this first.**
- **`roster_revision_2026-07.md` — roster revision v2 (role-based selection, AMZN/GOOGL exempt-hold retirement → legacy exits, international rotation governance). SUPERSEDES the fixed-24-ticker core roster (§2–§3) and exempt-hold doctrine (§8) of the strategy spec; resolves FOLLOWUPS #36.**
- **`Learning_Loop_v1.0.md` — the monthly strategy-review + human-approval amendment channel (proposer ≠ approver; Git/PR is the only change mechanism). Resolves FOLLOWUPS #13 + #32. See "Learning Loop" below.**
- `Data_Sources_Reference_v1.3.md` — all API endpoints, schemas, budget (supersedes v1.2, which is retained for history)
- `Storage_Architecture.md` — blob containers, table schemas, retention
- `Analyzer_Pipeline.md` — context assembly, memory, response parsing, Alpaca mapping
- `Phase_C_Performance_Feedback_v1.0.md` — self-measurement vs SPY + decision-outcome learning (Phase C, largely implemented; see FOLLOWUPS #7 + Done)
- `Flex_Catalyst_Engine_v1.0.md` — the intraday catalyst Flex engine (`src/flex/`, FLEX_ENABLED-gated; the active flex design)
- `Flex_Conviction_Sleeve_v1.0.md`, `Flex_Trailing_Stop_v1.0.md` — SUPERSEDED earlier flex designs, retained for history (see FOLLOWUPS #10)
- `Future_Project_Wheel_Strategy.md` — a future options-wheel project, not yet started

(There is **no** standalone "Architecture Spec" doc — system design/security/deployment lives in `growth_strategy_spec_v1.md`, this file's "Deployment lessons", and the Bicep in `infra/`.)
